# Upstream

Bug e problemi trovati in colibri e ds4 (e llama.cpp) mentre se ne portano i pezzi. **llama.cpp non
accetta testi scritti dall'AI**: i suoi bug restano qui come note, salvo che Marcello li porti di persona. Ogni riga dice la prova,
cosa si rompe davvero e a che punto è la segnalazione. Una PR o un'issue si apre solo dopo il sì
esplicito di Marcello, seguendo la skill `oss-contributo` nel fork di quel progetto
(`Desktop\colibri`, `Desktop\ds4`), con il suo cancello e le sue regole di contribuzione.

Decisione 2026-09-17: nessuna PR per ora, le segnalazioni si accumulano e si decide dopo.
2026-09-19: Marcello chiede di seguirle con la skill. Si portano fino a *PR pronta*; per aprire
serve comunque il suo sì. Lo stato fine di ognuna sta nello `STATO.md` del fork.

Stati: *trovato* → *verificato* (prova riprodotta, non già segnalato) → *PR pronta* (fix + gate
verde nel fork) → *aperta #n* → *chiusa* (mergiata o rifiutata, con il motivo).

| # | Data | Progetto | Cosa | Prova | Impatto reale | Stato |
|---|---|---|---|---|---|---|
| 1 | 2026-09-17 | ds4 `8db1d1d` | `gguf_types[]` in `ds4.c` dà dimensioni di blocco sbagliate a due tipi: `iq1_s` 256/110 (giusto 256/50) e `iq4_nl` 256/50 (giusto 32/18) | il `cuda/mmq/ggml-common.h` dello stesso repo lo verifica a compilazione (`static_assert(sizeof(block_iq1_s) == sizeof(ggml_half) + QK_K/8 + QK_K/16)`); `gguf.GGML_QUANT_SIZES` concorda; trovato dal lint di Trochilus | `tensor_nbytes` calcola la dimensione di ogni tensore da questa tabella: un GGUF con tensori `iq1_s` risulta più grande del vero e può essere rifiutato come fuori dal file; con `iq4_nl` la dimensione e il riepilogo dei tipi sono sbagliati. I modelli ufficiali di ds4 non usano questi tipi: tocca chi apre GGUF di terzi. Con `iq1_s` il loader si ferma con `ds4_die("tensor points outside GGUF file")` | **aperta #1095** 2026-09-19 (antirez/ds4): branch `gguf-iq-block-sizes`, test rosso→verde in `tests/test_session_state.c`, gate verde |
| 2 | 2026-09-17 | colibri `a90bed9` (dev) | `make check` con gcc 13 non è a 0 warning, contro la regola di CONTRIBUTING: `g_metal_prefill` (`colibri.c:3950`), `g_direct_heat_explicit` (`colibri.c:904`), `g_k3_usage` (`kimi_k3.c:191`) inutilizzati nei build con adattatori; `-Wformat-truncation` in `st.h:452`; `-Wmaybe-uninitialized` su `stats.total_bytes` in `deepseek_v4.c:1360` | log del gate di calibrazione nel fork (`Desktop\colibri\tmp\gate\dev\`) | nessun comportamento sbagliato: il `maybe-uninitialized` è un falso positivo (la funzione riempie sempre `stats` quando riesce), il troncamento colpisce solo percorsi oltre ~1170 caratteri e l'indice è un controllo incrociato. Solo igiene del build | verificato, valore basso; upstream accetta queste pulizie (PR #1458, #1123, #350 mergiate). **aperta #1624** 2026-09-19 (JustVugg/colibri, contro `dev`): branch `fix/build-warnings`, build fresca su `dev` `5a0b725` con gli stessi 5 warning, gate verde a 0 warning, test nuovo per il ramo di `st.h` |
| 3 | 2026-09-17 | llama.cpp `b49650a` | su Windows l'affinità dei thread arriva solo al primo gruppo di processori: `ggml_thread_apply_affinity` costruisce una maschera a 64 bit e chiama `SetThreadAffinityMask`, che vale dentro il gruppo corrente; oltre 64 processori logici stampa «setting thread-affinity for > 64 CPUs isn't supported on windows!» e li ignora | `ggml/src/ggml-cpu/ggml-cpu.c:2522` (il loro `TODO: support > 64 CPUs`), righe 2523-2554 | su una macchina Windows con più di 64 processori (due o più gruppi) `--cpu-mask` e `--cpu-strict` non possono nominare metà dei processori: il pin resta nel gruppo in cui il thread è nato. L'API che lo farebbe esiste dal 2009, `SetThreadGroupAffinity` con `GROUP_AFFINITY`: è quella che usa Trochilus | verificato (TODO loro, nessuna issue collegata); fermo 2026-09-19 come #5: policy AI di llama.cpp, Marcello «lasciamo stare» |
| 4 | 2026-09-17 | llama.cpp `b49650a` | i due blocchi Windows che contano i processori sono esclusi dai build MinGW-w64 (`&& !defined(__MINGW64__)`), che ricadono su `std::thread::hardware_concurrency()` | `common/common.cpp:116` (`common_cpu_get_num_physical_cores`) e `:428` (`common_params_get_system_info`) | `hardware_concurrency()` su Windows conta i processori del **gruppo corrente**, al massimo 64: un build MinGW su una macchina a più gruppi dimensiona la squadra su metà macchina e la stampa sbagliata. Sotto i 64 processori resta comunque `logici / 2` come stima dei core fisici, che sbaglia dove i core non sono tutti uguali (P/E-core) o dove SMT è spento. Trochilus compila proprio con MinGW-w64: è il motivo per cui la topologia se la conta da sé in `src/base/cpu.c` | verificato (TODO loro: «windows + arm64 + mingw64»); fermo 2026-09-19 come #5: policy AI di llama.cpp, Marcello «lasciamo stare» |
| 5 | 2026-09-17 | llama.cpp `b49650a` | il warning dell'affinità su Linux stampa il **puntatore** invece della maschera: `(unsigned long long)mask` dove `mask` è il parametro `const bool *` della funzione | `ggml/src/ggml-cpu/ggml-cpu.c:2662` | solo diagnostica, ma chi vede il warning legge un indirizzo di stack credendo sia la maschera rifiutata, cioè esattamente l'informazione per cui il messaggio esiste | verificato (nessuna issue collegata; ancora su `master` `e613ef2`, riga 2670). fermo 2026-09-19: la policy di llama.cpp (`AGENTS.md`, `CONTRIBUTING.md`) chiude le PR con descrizione, commit o risposte scritti dall'AI e chiede che chi contribuisce spieghi ogni riga da sé; il nostro metodo non ci sta. Nessun fork; Marcello: «lasciamo stare», anche per #3 e #4 |
| 6 | 2026-09-19 | ds4 `8db1d1d` | `tests/test_tp_commands.c:54` rilegge `SO_RCVTIMEO` e pretende il valore esatto al millisecondo | nel container `ds4-dev` (kernel WSL2, `CONFIG_HZ=250`, scatti da 4 ms): 50 ms rilegge 52, 1234 rilegge 1236, 3000 resta 3000 | solo il test: fallisce su ogni kernel a 250 Hz (Debian di serie, WSL2), e con lui `make test-session-state` e `make test`. Il codice di `ds4_tp.c` non sbaglia | **aperta #1096** 2026-09-19 (antirez/ds4): branch `tp-timeout-test-hz`, il test confronta con quello che il kernel tiene per lo stesso timeout; gate verde (asserzione entrata con `bd66c40` il 2026-09-12) |
| 7 | 2026-09-20 | colibri `a90bed9` | cache degli esperti di OLMoE: **una lettura a richiesta non sa di un precaricamento in volo dello stesso esperto**. `pilot_realload` nasconde lo slot (`cache_hide`, `eid = -1`), lascia il lucchetto e legge; `expert_get` nel frattempo cerca con `slot_indexed`, non trova, non guarda `is_queued` e carica lo stesso esperto in un secondo slot | lettura del codice, non riprodotto: `c/olmoe.c:678-742` (`expert_get`: nessun controllo di `is_queued` né degli slot in volo), `:1032-1090` (`pilot_realload`: `cache_hide` → unlock → `load_expert_merged` → `cache_publish`) | due letture dal disco dello stesso esperto proprio quando il disco è il collo, e due slot con lo stesso `eid` (uno non più indicizzato) finché l'LRU non lo sfratta | trovato |
| 8 | 2026-09-20 | colibri `a90bed9` | un errore di lettura in un **precaricamento facoltativo** chiude il processo: `pilot_realload` → `load_expert_merged` → `st_pread_full`, che fa `exit(1)` su errore e su lettura corta | lettura del codice: `c/st.h:261-281` (`exit(1)` nei due rami), `c/olmoe.c:1082` | un disco che sbaglia una lettura speculativa ferma l'inferenza, anche se quell'esperto non sarebbe mai servito | trovato |
| 9 | 2026-09-20 | colibri `a90bed9` | il thread di precaricamento serve **un solo modello per processo**: `pilot_m` è globale e lo fissa il primo `ensure_pilot_worker_started`; la coda `pilot_q` è condivisa, quindi le richieste di un secondo `Model` verrebbero risolte sulla cache del primo | lettura del codice: `c/olmoe.c:128-131` (globali), `:192-202`, `:1093-1108` (`pilot_realload(pilot_m, ...)`); il commento a `:569-570` dice che più segmenti in un processo sono previsti | solo con due `Model` con `PILOT` nello stesso processo; da verificare se oggi succede | trovato |
