# Lezioni

Ogni errore e ogni scoperta, appena succede. Una lezione è **chiusa** solo quando la sua
prevenzione è automatica: un test, un controllo di `make check`, un pin, un hook. Una frase
in un documento è una prevenzione debole e si dichiara come tale (colonna «Prevenzione»: *regola*).

«Trovato da» misura il tasso di successo: l'obiettivo è che nessun errore arrivi a Marcello
quando un test poteva trovarlo prima. Quando succede, la prevenzione deve essere un controllo.

Si aggiunge in coda. Una lezione superata si corregge sulla sua riga, non se ne scrive una accanto.

| # | Data | Tipo | Cosa è successo | Causa | Prevenzione | Stato | Trovato da |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-17 | errore | `import torch._dynamo` fallisce su Windows, transformers non carica OLMoE | numpy 2.5 non compatibile con torch 2.13 | pin `numpy<2.4` in `tools/requirements-oracle.txt` | chiusa | io, installando |
| 2 | 2026-09-17 | errore | `make` su MinGW cerca `cc`, che non esiste | GNU make ha un `CC=cc` predefinito, `CC ?= gcc` non scatta | `ifeq ($(origin CC),default)` nel Makefile | chiusa | io, primo build |
| 3 | 2026-09-17 | errore | test che usano `tmpfile()` falliscono su Windows | serve permesso di scrittura sulla radice del disco | `tools/lint.py` rifiuta `tmpfile(` nei test; i test scrivono accanto al proprio binario | chiusa | test su Windows |
| 4 | 2026-09-17 | errore | il pool di thread si bloccava o saltava indici | due race: worker avviato dopo il primo lavoro; worker su una generazione vecchia che rifaceva un lavoro finito | `test_parallel_varying_chunks` in `tests/test_base.c` (20.000 lanci, pezzi variabili); non provato sul codice vecchio, che non c'è più | chiusa | agente, stress test |
| 5 | 2026-09-17 | errore | un agente ha lasciato un container Docker appeso (ThreadSanitizer bloccato) | TSan non funziona nel container `colibri-dev`; nessuna pulizia a fine lavoro | *regola* nei brief: «rimuovi i container, nessun processo in background»; `docker ps` dopo ogni agente | aperta: serve un controllo automatico a fine agente | io, controllando i processi |
| 6 | 2026-09-17 | errore | `docker run -w /src` da Git Bash diventa `C:/Program Files/Git/src` | Git Bash converte i percorsi POSIX | `MSYS_NO_PATHCONV=1` davanti a docker | *regola* | io |
| 7 | 2026-09-17 | scoperta | clang nel container non linka ASan | l'immagine non ha compiler-rt | i sanitizer si fanno con gcc | *regola* | io |
| 8 | 2026-09-17 | errore | un conteggio di warning con grep contava 6 falsi errori in un build pulito | il pattern prendeva `-Werror` nelle righe di comando | `make check` compila con `WERROR=1`: un warning fa fallire il build, niente da contare | chiusa | io |
| 9 | 2026-09-17 | scoperta | OLMoE in transformers 5.x: `q_norm` su tutta la proiezione, esperti salvati fusi (`gate_up_proj`) | il modello non è per-testa come Qwen3 | `make oracle` confronta con transformers a ogni `make check` (16/16 token) | chiusa | lettura del sorgente |
| 10 | 2026-09-17 | errore | la tabella dei tipi presa da ds4 aveva due dimensioni sbagliate: iq4_nl (256/50 invece di 32/18) e iq1_s (256/110 invece di 256/50) | errori nella tabella d'origine, tipi che ds4 non usa | `tools/lint.py` confronta la tabella e la numerazione con `gguf.GGML_QUANT_SIZES` (è il controllo che ha trovato iq1_s) | chiusa | io (iq4_nl), lint (iq1_s) |
| 11 | 2026-09-17 | scoperta | 16 thread su una matrice da 1 ms rendono solo 3×, rumore 74%; con i kernel AVX-512 una matrice da 0.1 ms va più piano a 16 thread che a 1, e il modello vero fa 17.7 tok/s a 16 thread, 21.1 a 8, 13.9 a 1 | svegliare i thread costa quanto il calcolo, 464 volte per token | i thread vanno sui blocchi grandi (gli esperti del token, lo strato); scenario a più thread in `bench/scenarios.json` quando si cambia | aperta | microbenchmark, profilo del modello vero |
| 12 | 2026-09-17 | errore | Smart App Control (attivo, stato 1) blocca i `.exe` appena compilati per minuti: 40 blocchi in 3 ore, un agente fermo 20 minuti; anche `uv` lanciato diretto | politica di Windows sui binari senza reputazione | la correttezza di `make check` gira in Linux (immagine `trochilus-dev`); su Windows si compila con 0 warning e si misura quando il binario passa | chiusa per il cancello, aperta per le misure native | agenti, io |
| 13 | 2026-09-17 | debito | `argv` su Windows non è UTF-8: un percorso o un prompt con accenti arriva rovinato | MinGW passa la code page locale | `wmain` con `-municode` e conversione in UTF-8 (`tr_utf8_argv`); `make check` su Windows lancia `tokenize -p "città è perché"` e vuole gli id di transformers (scrive SKIPPED se Smart App Control blocca l'exe, #12) | chiusa | io, rileggendo `main.c` |
| 14 | 2026-09-17 | errore | barre rovesciate scritte da shell o Python diventano tab e a capo: rovinati la mappa dei progetti, questa riga e `tools/lint.py` (tre volte) | una barra rovesciata seguita da t o r diventa un carattere di controllo | `tools/lint.py` rifiuta tab e ritorni a capo in CLAUDE.md e docs; nel codice, testo con barre rovesciate solo con lo strumento Edit/Write | chiusa per i documenti, *regola* per il codice; ripetuta altre 3 volte il 2026-09-17 (benchmark, Makefile): la doppia barra nel comando arriva a Python già dimezzata, e l'hook della #18 non era attivo in questa sessione | io, rileggendo; il compilatore e `grep` |
| 15 | 2026-09-17 | errore | `tools/oracle.py` non trovava `build/trochilus.exe` su Windows pur esistendo | `subprocess` di Windows non risolve un percorso relativo con barre in avanti | percorso reso assoluto con `os.path.abspath` in `oracle.py`; `make oracle` lo esercita a ogni check | chiusa | agente |
| 16 | 2026-09-17 | scoperta | lanciando `trochilus.exe` a mano da Git Bash con un percorso a barra singola in testa (`/tmp_x.json`) l'argomento arriva riscritto sotto `C:/Program Files/Git/...` | Git Bash converte ogni argomento che sembra un percorso POSIX assoluto, non solo `-w` di docker (lezione #6) | *regola*: da Git Bash si passano a `trochilus` percorsi relativi o con lettera di unità, mai barra singola in testa; `tools/profile_suite.py` non ne risente, i percorsi li costruisce con `os.path`/`tempfile` | aperta | io, testando `generate --profile-json` a mano |
| 17 | 2026-09-17 | errore | `trochilus generate` usciva con successo anche quando il contesto finiva a metà, restituendo meno token del richiesto | il ciclo di generazione faceva `break` e il codice di uscita restava 0 | codice di uscita 3 per contesto pieno; `make check` verifica che `-c 8` con 6+4 token fallisca e `-c 16` riesca | chiusa | test nuovo in `make check` |
| 18 | 2026-09-17 | errore | la lezione 14 si è ripetuta per la quarta volta, dentro `main.c` | una regola scritta non ferma un gesto automatico | hook `.claude/hooks/niente-barre-in-scritture-shell.cjs`: rifiuta i comandi che scrivono file da shell/Python con barre rovesciate (provato su 5 casi) | aperta: gli hook del progetto si caricano solo se Claude Code parte dalla cartella `trochilus`; la sessione del 2026-09-17 partiva dal Desktop e non li ha mai avuti, `/hooks` non basta (verificato con un comando che doveva essere bloccato). *Regola*: aprire Claude Code dentro `trochilus`. Sessione successiva aperta in `trochilus`: il comando di prova è bloccato, e l'hook ha fermato anche una scrittura vera con `\n` | io; Marcello ha aperto `/hooks` e il test ha mostrato il motivo vero |
| 19 | 2026-09-17 | errore | gcc salva i registri AVX in array sullo stack con istruzioni che pretendono allineamento a 64 byte: su MinGW lo stack è allineato a 16 (bug di gcc 54412), e sotto ASan lo stack finto per `use-after-return` non è allineato: `test_kernels` in crash nel kernel AVX-512 | gcc si fida di un allineamento che due ambienti non garantiscono | `Makefile`: con gcc, su ogni sistema, `-Wa,-muse-unaligned-vector-move` (l'assembler riscrive le istruzioni allineate in non allineate, stessa velocità; binutils >= 2.38); il cancello esegue i kernel AVX sotto ASan con le opzioni predefinite, `make bench` li esegue nativi su Windows | chiusa | `make check` (ASan), dopo che l'avevo prevista solo per Windows |
| 20 | 2026-09-17 | errore | il test «ogni tier = scalare» passava anche con due corsie scambiate dentro un kernel AVX2 | valori speciali in 1 elemento su 16: quasi ogni corsia conteneva un infinito o un NaN, e un NaN uguale a un altro NaN nasconde ogni differenza di arrotondamento | valori speciali rari e solo nei giri dispari; `test_kernels` contiene una variante sbagliata apposta (corsie scambiate) che il confronto deve vedere, o il test fallisce; provati a mano anche FMA e byte sbagliati | chiusa | io, rompendo apposta i kernel |
| 21 | 2026-09-17 | errore | aggiunto un flag nel `Makefile`, il test sotto ASan andava ancora in crash: gli oggetti erano quelli vecchi | le regole `.o` dipendevano solo dal `.c` e dagli header, non dai flag | ogni `.o` dipende dal `Makefile`: un flag cambiato ricompila tutto | chiusa | io, contando le istruzioni allineate rimaste nell'oggetto |
| 22 | 2026-09-17 | debito | con 11 GB liberi il motore ha caricato 7.4 GB di pesi e solo dopo ha rifiutato la sessione da 0.03 GB (sarebbero rimasti 2.96 GB, minimo 3.12) | il controllo della memoria al caricamento non somma la sessione che verrà creata | stimare pesi + sessione minima prima di leggere il file; test con un modello sintetico e una soglia finta | aperta | io, misurando il modello vero |
| 23 | 2026-09-17 | scoperta | una mutazione di prova (`free(malloc(64))` dentro la generazione) non faceva fallire il test delle allocazioni | a `-O2` gcc toglie una coppia allocazione-rilascio che non serve a niente: la mutazione non allocava davvero | le mutazioni di prova e la sonda del test usano un puntatore `volatile`, che il compilatore non può togliere; `test_hot` verifica prima che il contatore veda una sonda vera | chiusa | io, rompendo apposta il motore |
| 24 | 2026-09-17 | errore | misurato il pool nuovo con un `bench_kernels` vecchio: numeri identici a prima, sembrava che il cambio non servisse | `make test` non ricompila il benchmark, e l'ho lanciato direttamente | il cancello ricompila `bench_kernels` in Linux; *regola*: le misure si lanciano con `make bench` o `make profile`, che ricompilano | aperta: serve che il benchmark stampi il commit o l'ora di compilazione | io, vedendo numeri uguali al millesimo |
| 25 | 2026-09-17 | scoperta | ThreadSanitizer non parte nel container: «unexpected memory mapping» | il kernel di WSL2 randomizza la memoria con più bit di quanti TSan ne supporti | nel cancello i test TSan girano con `setarch -R` e il container con `--security-opt seccomp=unconfined`; TSan su `test_base` e `test_hot` a ogni `make check` | chiusa | io, primo tentativo di TSan sul pool nuovo |
| 26 | 2026-09-17 | scoperta | l'hook contro le barre rovesciate (#18) guarda solo lo strumento Bash: un `python -c "...write('a\nb')"` lanciato dallo strumento PowerShell passa, e il file si rovina allo stesso modo | il matcher dell'hook in `.claude/settings.json` è `"Bash"` | matcher `"Bash\|PowerShell"` (il comando è nello stesso campo `tool_input.command`), applicato col sì di Marcello: da PowerShell la scrittura con `\n` è bloccata, quella senza passa | chiusa | io, verificando l'hook |
| 27 | 2026-09-17 | scoperta | spostare rope, attivazione ed attenzione non ha cambiato un bit dei logit del modello vero (200 token, 16/3/1 thread, scalare contro AVX-512) | operazioni elemento per elemento e somme nello stesso ordine: la divisione in thread non tocca i numeri | `test_kernels`: rope da tabella = rope con `pow`/`cos`/`sin` per elemento, swiglu con 1/2/3/8 thread = seriale, `tr_attention_head` = ciclo di prima, `axpy_f32` di ogni tier = scalare (con una variante FMA che deve risultare diversa); le tre mutazioni di prova sono state prese | chiusa | io, confronto dei logit prima/dopo |
| 28 | 2026-09-17 | errore | la prima stesura del tokenizer teneva il contesto dell'ordinamento degli added token in una variabile statica: due tokenizer caricati insieme da due thread si sarebbero pestati | `qsort` non ha un argomento di contesto, e la scorciatoia è stata una variabile globale | `tools/lint.py` rifiuta ogni variabile statica mutabile in `src/` (puntatori compresi) senza la nota `global-ok: motivo`, dopo aver provato su campioni che il controllo le vede; le 13 esistenti sono annotate come fatti del processo | chiusa | io, rileggendo prima di compilare |
| 29 | 2026-09-17 | errore | il piano del tokenizer citava solo colibri `tok.h`; ds4, che legge il vocabolario dal GGUF come serve a noi, è stato letto solo quando Marcello l'ha chiesto | la colonna «da dove viene l'idea» di `docs/ARCHITETTURA.md` è stata scritta una volta sola, senza confrontare le due implementazioni componente per componente | da fare: una tabella di confronto per componente in `docs/ORIGINI.md` (cosa fa colibri, cosa fa ds4, difetti, cosa si prende, cosa si migliora e con quale prova) e un controllo in `tools/lint.py` che la pretende per ogni cartella di `src/` | aperta | Marcello |
| 30 | 2026-09-17 | scoperta | il tokenizer di OLMoE in transformers normalizza in NFC prima di tokenizzare; llama.cpp no, e nemmeno colibri e ds4: su testo con accenti scomposti (nomi di file da macOS) i token cambiano | `normalizer: NFC` in `tokenizer.json`, che il GGUF non registra | la famiglia `olmo` porta `nfc = 1` nella tabella di `tokenizer.c`; `test_tokenizer` (e + U+0301 = é); l'oracolo confronta il testo normalizzato e fa lo sweep della forma NFD di ogni codepoint | chiusa | io, leggendo `tokenizer.json` |
| 31 | 2026-09-17 | scoperta | il nostro convertitore scriveva un vocabolario diverso da quello dei GGUF veri: tutti gli added token CONTROL, pre "default", niente `[PAD]` | `gguf.BpeVocab` di gguf-py non è la logica di `convert_hf_to_gguf.py` di llama.cpp | `hf_to_gguf.py` rifà `get_vocab_base` di llama.cpp; `make oracle-tokenizer` confronta ogni chiave `tokenizer.*` con il GGUF del modello vero quando c'è (0 differenze) | chiusa | agente, confronto con il GGUF vero |
| 32 | 2026-09-17 | scoperta | `tokenizers` 0.22.2 non usa Unicode 15.1: non scompone U+11938 e la sua tabella delle classi combinanti tratta come 0 112 segni recenti; tabelle prese da Python avrebbero dato token diversi | HF porta tabelle Unicode proprie e più vecchie | `tools/gen_unicode_tables.py` sonda HF, confronta la sua copia dell'algoritmo con HF su ogni codepoint, coppia e 25 000 sequenze, e non scrive se c'è una differenza; l'oracolo fa lo sweep di tutti i codepoint | chiusa | agente, generatore delle tabelle |
| 33 | 2026-09-17 | errore | il tokenizer rifiutava il GGUF vero di OLMoE: «no token for byte 0xc0» | ho dato per scontato che ogni vocabolario byte-level abbia i 256 byte; GPT-NeoX non ha i 13 byte che l'UTF-8 valido non contiene | il byte senza token si scarta prima dei merge, come HF; `test_missing_byte`; l'oracolo sul vocabolario vero gira in `make check` | chiusa | oracolo, primo giro |
| 34 | 2026-09-17 | errore | con `--no-parse-special` 2 testi davano token diversi da HF: IP_ADDRESS seguito subito da EMAIL_ADDRESS, che condividono le barre | saltavo i token di controllo durante la ricerca; HF li trova comunque e ne consuma il testo, così un token utente sovrapposto non si trova | il token di controllo non analizzato consuma il suo testo; `test_tokenizer` con un token utente che sta dentro il testo di un token di controllo; l'oracolo gira anche senza parse-special | chiusa | oracolo, primo giro |
| 35 | 2026-09-17 | errore | la prova delle mutazioni del tokenizer è rimasta appesa 10 minuti | una mutazione creava un pezzo di lunghezza zero e un ciclo infinito, e i test giravano senza limite di tempo | `split_gpt2` fa avanzare ogni pezzo di almeno un codepoint; `make test` lancia ogni test con `timeout 300` | chiusa | io, prova delle mutazioni |
| 36 | 2026-09-17 | errore | il controllo degli accenti da riga di comando falliva dentro `make check` ma passava da Git Bash | GNU make su Windows passa la ricetta alla shell nella code page locale: «città» arrivava rovinato prima del motore | il testo sta in `tools/check_argv_utf8.sh`; `tools/lint.py` rifiuta caratteri non ASCII nelle ricette del Makefile | chiusa | `make check`, primo giro del controllo |
| 37 | 2026-09-17 | errore | `trochilus chat` non partiva sul PC di Marcello: «need 1.00 GiB, only 3.68 GiB available» | la chat chiedeva sempre la cache per tutto il contesto di addestramento (4096 token, 1 GB); con i container Docker degli altri progetti accesi la RAM libera non bastava, e l'utente doveva conoscere `-c` | senza `-c` la chat dimezza il contesto (fino a 512) finché la guardia della memoria accetta, e lo dice; provato occupando RAM apposta (4096 e 2048 rifiutati, 1024 accettato, risposta regolare). Manca un test automatico: arriva con il piano automatico della memoria (M1), che deve poter ricevere un budget finto | aperta | Marcello |
| 38 | 2026-09-17 | errore | dopo le prove pesanti nel container (build di llama.cpp, tre caricamenti del modello) su Windows restavano 2,65 GB di RAM libera | la macchina virtuale di Docker tiene i file letti nella sua cache e non restituisce la memoria a Windows | `make check` su Windows svuota la cache della VM alla fine (`drop_caches`); *regola*: stessa cosa dopo ogni prova pesante lanciata a mano nel container | aperta: le prove a mano non sono coperte da un controllo | io, prova nativa rifiutata dalla guardia della memoria |
| 39 | 2026-09-17 | scoperta | llama.cpp non è un riferimento esatto per i logit: sullo stesso Q8_0 la KL media contro Trochilus è 9e-3 e la generazione greedy si separa dopo 19 token | llama.cpp quantizza a 8 bit anche le attivazioni e tiene la KV in f16 | il riferimento esatto resta transformers: oracolo sugli stessi pesi reali, modello tagliato a 2 layer (prossimo passo) | aperta | io, `tools/compare_llamacpp.py` |
