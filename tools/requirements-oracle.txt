# Riferimento di correttezza: modelli minuscoli con pesi casuali eseguiti da HF transformers.
# Solo per gli strumenti in tools/: il motore non dipende da Python.
# Stessi pin di colibri (c/tools/requirements-deepseek-v4-tiny.txt @ a90bed9).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu; sys_platform != "darwin"
torch==2.13.0; sys_platform == "darwin"
transformers==5.14.1
safetensors==0.8.0
# numpy 2.5 rompe `import torch._dynamo` di torch 2.13 su Windows (import circolare su NP_SUPPORTED_MODULES)
numpy>=1.24,<2.4
# gguf-py di llama.cpp (MIT): GGUFWriter/GGUFReader, tabelle Keys e nomi tensori per architettura
gguf==0.19.0
