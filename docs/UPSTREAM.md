# Upstream

Bugs and issues found in colibri, ds4 (and llama.cpp) while porting code from them.
**llama.cpp does not accept text written by AI**: its bugs remain here as notes, unless
Marcello carries them over personally. Each row states the test case, what actually breaks,
and the status of the report. A PR or issue is opened only after explicit approval from
Marcello, following the `oss-contributo` skill in that project's fork (`Desktop\colibri`,
`Desktop\ds4`), with its gate and contribution rules.

Decision 2026-09-17: no PRs for now; reports accumulate and a decision is made later.
2026-09-19: Marcello asks to track them with the skill. They are brought to *PR ready*;
opening still requires his approval. The final status of each is in the fork's `STATUS.md`.

States: *found* → *verified* (test case reproduced, not already reported) → *PR ready*
(fix + green gate in fork) → *opened #n* → *closed* (merged or rejected, with reason).

| # | Date | Project | What | Test Case | Real Impact | State |
|---|---|---|---|---|---|---|
| 1 | 2026-09-17 | ds4 `8db1d1d` | `gguf_types[]` in `ds4.c` gives wrong block sizes for two types: `iq1_s` 256/110 (correct 256/50) and `iq4_nl` 256/50 (correct 32/18). A third, `q8_1` 32/40 (correct 32/36), found by emilianbold in review | The `cuda/mmq/ggml-common.h` in the same repo verifies it at compile time (`static_assert(sizeof(block_iq1_s) == sizeof(ggml_half) + QK_K/8 + QK_K/16)`); `gguf.GGML_QUANT_SIZES` agrees; found by Trochilus's lint | `tensor_nbytes` calculates the size of each tensor from this table: a GGUF with `iq1_s` tensors appears larger than it is and can be rejected as outside file; with `iq4_nl` the size and type summary are wrong. Official ds4 models do not use these types: it affects those opening third-party GGUFs. With `iq1_s` the loader stops with `ds4_die("tensor points outside GGUF file")` | **opened #1095** 2026-09-19 (antirez/ds4): branch `gguf-iq-block-sizes`, test red→green in `tests/test_session_state.c`, green gate. `q8_1` added 2026-09-21 with `12f0a57`; **LGTM** from emilianbold 2026-09-22 (triage only, cannot merge): waiting for antirez |
| 2 | 2026-09-17 | colibri `a90bed9` (dev) | `make check` with gcc 13 is not at 0 warnings, against the CONTRIBUTING rule: `g_metal_prefill` (`colibri.c:3950`), `g_direct_heat_explicit` (`colibri.c:904`), `g_k3_usage` (`kimi_k3.c:191`) unused in adapter builds; `-Wformat-truncation` in `st.h:452`; `-Wmaybe-uninitialized` on `stats.total_bytes` in `deepseek_v4.c:1360` | Log from gate run in fork (`Desktop\colibri\tmp\gate\dev\`) | No wrong behavior: `maybe-uninitialized` is a false positive (function always fills `stats` when it succeeds), truncation hits only paths over ~1170 chars, and the index is a cross-check. Build hygiene only | **closed: merged** 2026-09-22 by JustVugg into `dev` (`11106c9`), no review comments. Was #1624 (JustVugg/colibri), opened 2026-09-19: branch `fix/build-warnings`, green gate at 0 warnings, new test for `st.h` branch; macOS CI red on 2026-09-20 (`PATH_MAX` 1024), fixed with `9ec0f55` |
| 3 | 2026-09-17 | llama.cpp `b49650a` | On Windows thread affinity reaches only the first processor group: `ggml_thread_apply_affinity` builds a 64-bit mask and calls `SetThreadAffinityMask`, which works within the current group; beyond 64 logical processors it prints "setting thread-affinity for > 64 CPUs isn't supported on windows!" and ignores them | `ggml/src/ggml-cpu/ggml-cpu.c:2522` (their `TODO: support > 64 CPUs`), lines 2523-2554 | On a Windows machine with more than 64 processors (two or more groups) `--cpu-mask` and `--cpu-strict` cannot name half the processors: the pin stays in the group where the thread was born. The API to do it exists since 2009, `SetThreadGroupAffinity` with `GROUP_AFFINITY`: that is what Trochilus uses | verified (TODO theirs, no linked issue); halted 2026-09-19 as #5: llama.cpp AI policy, Marcello "leave it" |
| 4 | 2026-09-17 | llama.cpp `b49650a` | The two Windows blocks that count processors are excluded from MinGW-w64 builds (`&& !defined(__MINGW64__)`), falling back to `std::thread::hardware_concurrency()` | `common/common.cpp:116` (`common_cpu_get_num_physical_cores`) and `:428` (`common_params_get_system_info`) | `hardware_concurrency()` on Windows counts processors in the **current group**, max 64: a MinGW build on a multi-group machine sizes the pool to half the machine and prints it wrong. Under 64 processors it still uses `logics / 2` as physical core estimate, which fails where cores are unequal (P/E-core) or SMT is off. Trochilus compiles with MinGW-w64 itself: that is why it counts topology in `src/base/cpu.c` | verified (TODO theirs: "windows + arm64 + mingw64"); halted 2026-09-19 as #5: llama.cpp AI policy, Marcello "leave it" |
| 5 | 2026-09-17 | llama.cpp `b49650a` | The affinity warning on Linux prints the **pointer** instead of the mask: `(unsigned long long)mask` where `mask` is the function's `const bool *` parameter | `ggml/src/ggml-cpu/ggml-cpu.c:2662` | Diagnostics only, but whoever sees the warning reads a stack address thinking it is the rejected mask, i.e. exactly the information this message exists to show | verified (no linked issue; still on `master` `e613ef2`, line 2670). Halted 2026-09-19: llama.cpp policy (`AGENTS.md`, `CONTRIBUTING.md`) closes PRs with AI-written description, commits, or replies, and asks contributors to explain every line themselves; our method does not fit. No fork; Marcello: "leave it", also for #3 and #4 |
| 6 | 2026-09-19 | ds4 `8db1d1d` | `tests/test_tp_commands.c:54` re-reads `SO_RCVTIMEO` and demands exact value in milliseconds | In `ds4-dev` container (WSL2 kernel, `CONFIG_HZ=250`, ticks 4 ms): 50 ms re-reads 52, 1234 re-reads 1236, 3000 stays 3000 | Test only: fails on any 250 Hz kernel (Debian stock, WSL2), and with it `make test-session-state` and `make test`. Code in `ds4_tp.c` is fine | **opened #1096** 2026-09-19 (antirez/ds4): branch `tp-timeout-test-hz`, test compares with what the kernel keeps for same timeout; green gate (assertion entered `bd66c40` on 2026-09-12) |
| 7 | 2026-09-20 | colibri `a90bed9` | OLMoE expert cache: **an on-demand read does not know about an in-flight prefetch of the same expert**. `pilot_realload` hides the slot (`cache_hide`, `eid = -1`), leaves the lock, and reads; `expert_get` meanwhile searches with `slot_indexed`, finds nothing, does not check `is_queued` and loads the same expert in a second slot | Code read, not reproduced: `c/olmoe.c:678-742` (`expert_get`: no check of `is_queued` or in-flight slots), `:1032-1090` (`pilot_realload`: `cache_hide` → unlock → `load_expert_merged` → `cache_publish`) | Two disk reads of the same expert exactly when disk is the bottleneck, and two slots with the same `eid` (one no longer indexed) until LRU evicts it | **closed: merged** 2026-09-23 by JustVugg into `dev`, no review comments. Was **#1693**, opened 2026-09-22 (JustVugg/colibri, against `dev`): still present on `dev` `9e152d4`; also the reverse (prefetch while demand reads) and demand→demand. Branch `fix/olmoe-inflight-dedup`: reservation `-(eid+2)` + `g_pilot_cv` as in `colibri.c` `ecache_reserve`; 3 scenarios in `tests/test_olmoe_cache_index.c`, 7 FAIL on `dev`, green after, 6 mutations caught; gate green except the #10 warning |
| 8 | 2026-09-20 | colibri `a90bed9` | A read error in a **speculative prefetch** closes the process: `pilot_realload` → `load_expert_merged` → `st_pread_full`, which does `exit(1)` on error and short read | Code read: `c/st.h:261-281` (`exit(1)` in both branches), `c/olmoe.c:1082` | A disk that fails a speculative read stops inference, even if that expert would never be needed | found |
| 9 | 2026-09-20 | colibri `a90bed9` | The prefetch thread serves **only one model per process**: `pilot_m` is global and fixed by first `ensure_pilot_worker_started`; queue `pilot_q` is shared, so requests from a second `Model` would be resolved on the first model's cache | Code read: `c/olmoe.c:128-131` (globals), `:192-202`, `:1093-1108` (`pilot_realload(pilot_m, ...)`); comment at `:569-570` says multiple segments per process are intended | Only with two `Model` with `PILOT` in the same process; need to verify if it happens today | found |
| 10 | 2026-09-22 | colibri `9e152d4` (dev) | `make check` with gcc 13 is again not at 0 warnings: `qwen36.c:2822: 'serve_echo' used but never defined`, from `e16146c` (brio mode, 2026-09-20). `serve_echo` is defined inside `#ifndef QWEN36_NO_MAIN` (`qwen36.c:3258`) but declared and called outside it (`:2822`, `:2869`) | Gate log in fork (`tmp/gate/fix_olmoe-inflight-dedup/20260922-230634/check.log:230`) | Build hygiene in the segment build (`QWEN36_NO_MAIN`); the call is unreachable there or it would not link | **closed: merged** 2026-09-23 by JustVugg into `dev`, no review comments. Was **#1695**, opened 2026-09-22 (JustVugg/colibri, against `dev`): branch `fix/qwen36-echo-guard`, echo state and block under `#ifndef QWEN36_NO_MAIN`; full build byte-identical to `dev` (`gcc -E -P`), green gate at 0 warnings |
| 11 | 2026-09-21 | ds4 `8db1d1d` | `ggml_type_size()` in `cuda/mmq/ds4_ggml_stubs.h` is wrong for two types: `nvfp4` gives 18 bytes where `block_nvfp4` is 4+32 = 36, and `q1_0` gives 36 bytes on a 256-weight block where ggml has 128 weights in 18 bytes. Found while reviewing #1095, not while porting | `cuda/mmq/ggml-common.h:217` (`block_nvfp4`), `gguf-tools/quants.c:72`; code read, not reproduced (CUDA, no GPU here) | `mmvq.cu:1064-1066` uses `ggml_type_size` as row stride: an `nvfp4` tensor would be walked with half the right stride. The official models do not use these types | verified by reading; separate PR offered to emilianbold in the #1095 thread, no answer yet |
| 12 | 2026-09-21 | ds4 `8db1d1d` | `gguf_types[]` in `ds4.c` lacks `tq1_0` (34), `tq2_0` (35), `nvfp4` (40), `q1_0` (41), which `gguf-tools/quants.c` already knows. Found while reviewing #1095 | `ds4.c` `gguf_types[]` against `gguf-tools/quants.c` | Missing support, not a wrong size: `tensor_nbytes` fails closed (`block_elems == 0`) and the loader rejects the file | verified by reading; separate PR offered in the same thread, no answer yet |
