# Lessons

Every error and discovery, as soon as it happens. A lesson is **closed** only when its
prevention is automatic: a test, a check in `make check`, a pin, a hook. A phrase
in a document is weak prevention and is declared as such (column «Prevention»: *rule*).

«Found by» measures the success rate: the goal is that no error reaches Marcello
when a test could have found it first. When it does, the prevention must be a check.

Appended at the tail. A lesson that is superseded is corrected on its own line, not written
alongside a new one.

| # | Date | Type | What happened | Cause | Prevention | Status | Found by |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-17 | error | `import torch._dynamo` fails on Windows, transformers cannot load OLMoE | numpy 2.5 incompatible with torch 2.13 | pin `numpy<2.4` in `tools/requirements-oracle.txt` | closed | me, during install |
| 2 | 2026-09-17 | error | `make` on MinGW looks for `cc`, which does not exist | GNU make has a default `CC=cc`, `CC ?= gcc` does not trigger | `ifeq ($(origin CC),default)` in Makefile | closed | me, first build |
| 3 | 2026-09-17 | error | tests using `tmpfile()` fail on Windows | need write permission on disk root | `tools/lint.py` rejects `tmpfile(` in tests; tests write next to their own binary | closed | test on Windows |
| 4 | 2026-09-17 | error | thread pool would block or skip indices | two races: worker started after first job; worker on old generation re-doing finished job | `test_parallel_varying_chunks` in `tests/test_base.c` (20,000 launches, varying chunks); not tested on old code, which is gone | closed | agent, stress test |
| 5 | 2026-09-17 | error | an agent left a Docker container hung (ThreadSanitizer blocked) | TSan does not work in `colibri-dev` container; no cleanup at end of work | *rule* in briefs: «remove containers, no background processes»; `docker ps` after each agent | open: need automatic check at end of agent | me, checking processes |
| 6 | 2026-09-17 | error | `docker run -w /src` from Git Bash becomes `C:/Program Files/Git/src` | Git Bash converts POSIX paths | `MSYS_NO_PATHCONV=1` before docker | *rule* | me |
| 7 | 2026-09-17 | discovery | clang in container does not link ASan | image does not have compiler-rt | sanitizers done with gcc | *rule* | me |
| 8 | 2026-09-17 | error | a warning count with grep counted 6 false errors in a clean build | pattern picked `-Werror` in command lines | `make check` compiles with `WERROR=1`: a warning breaks build, nothing to count | closed | me |
| 9 | 2026-09-17 | discovery | OLMoE in transformers 5.x: `q_norm` over entire projection, experts saved fused (`gate_up_proj`) | model is not per-head like Qwen3 | `make oracle` compares with transformers at each `make check` (16/16 tokens) | closed | reading source |
| 10 | 2026-09-17 | error | type table taken from ds4 had two wrong dimensions: iq4_nl (256/50 instead of 32/18) and iq1_s (256/110 instead of 256/50) | errors in source table, types ds4 does not use | `tools/lint.py` compares table and numbering with `gguf.GGML_QUANT_SIZES` (it found iq1_s) | closed | me (iq4_nl), lint (iq1_s) |
| 11 | 2026-09-17 | discovery | 16 threads on a 1 ms matrix gives only 3×, noise 74%; with AVX-512 kernels a 0.1 ms matrix runs slower at 16 threads than at 1, and the real model does 17.7 tok/s at 16 threads, 21.1 at 8, 13.9 at 1 | waking threads costs as much as computation, 464 times per token | threads go to large blocks (token's experts, layer); scenario with more threads in `bench/scenarios.json` when changed | open | microbench, real model profile |
| 12 | 2026-09-17 | error | Smart App Control (active, state 1) blocks `.exe` just compiled for minutes: 40 blocks in 3 hours, one agent stuck 20 minutes; also `uv` run directly | Windows policy on binaries without reputation | correctness of `make check` runs on Linux (image `trochilus-dev`); on Windows compiles with 0 warnings and measures when binary passes | closed for gate, open for native measures | agents, me |
| 13 | 2026-09-17 | debt | `argv` on Windows is not UTF-8: a path or prompt with accents arrives corrupted | MinGW passes local code page | `wmain` with `-municode` and UTF-8 conversion (`tr_utf8_argv`); `make check` on Windows launches `tokenize -p "città è perché"` and wants transformers ids (writes SKIPPED if Smart App Control blocks exe, #12) | closed | me, re-reading `main.c` |
| 14 | 2026-09-17 | error | backslashes written by shell or Python become tabs and newlines: corrupted project map, this line and `tools/lint.py` (three times) | a backslash followed by t or r becomes a control character | `tools/lint.py` rejects tabs and newlines in CLAUDE.md and docs; in code, text with backslashes only with Edit/Write tool | closed for docs, *rule* for code; repeated 3 more times 2026-09-17 (benchmark, Makefile): double backslash in command already halved at Python, and hook #18 not active in this session | me, re-reading; compiler and `grep` |
| 15 | 2026-09-17 | error | `tools/oracle.py` could not find `build/trochilus.exe` on Windows though it existed | Windows `subprocess` does not resolve relative path with forward slashes | path made absolute with `os.path.abspath` in `oracle.py`; `make oracle` exercises it at each check | closed | agent |
| 16 | 2026-09-17 | discovery | running `trochilus.exe` by hand from Git Bash with a path starting with single slash (`/tmp_x.json`) argument arrives rewritten under `C:/Program Files/Git/...` | Git Bash converts every argument that looks like an absolute POSIX path, not just docker's `-w` (lesson #6) | *rule*: from Git Bash pass to `trochilus` relative paths or with drive letter, never leading single slash; `tools/profile_suite.py` unaffected, builds paths with `os.path`/`tempfile` | open | me, testing `generate --profile-json` by hand |
| 17 | 2026-09-17 | error | `trochilus generate` exited successfully even when context ran out halfway, returning fewer tokens than requested | generation loop did `break` and exit code stayed 0 | exit code 3 for full context; `make check` verifies `-c 8` with 6+4 tokens fails and `-c 16` succeeds | closed | new test in `make check` |
| 18 | 2026-09-17 | error | lesson 14 repeated a fourth time, inside `main.c` | a written rule does not stop an automatic action | hook `.claude/hooks/no-backslashes-in-shell-writes.cjs`: rejects commands writing files from shell/Python with backslashes (tested on 5 cases) | open: project hooks load only if Claude Code starts in `trochilus` folder; 2026-09-17 session started from Desktop and never had them, `/hooks` not enough (verified with command that should have been blocked). *Rule*: open Claude Code inside `trochilus`. Next session opened in `trochilus`: test command is blocked, and hook stopped an actual write with `\n` | me; Marcello opened `/hooks` and test showed real reason |
| 19 | 2026-09-17 | error | gcc saves AVX registers in array on stack with instructions requiring 64-byte alignment: on MinGW stack is aligned to 16 (gcc bug 54412), and under ASan fake stack for `use-after-return` not aligned: `test_kernels` crashes in AVX-512 kernel | gcc trusts alignment that two environments do not guarantee | `Makefile`: with gcc, on every system, `-Wa,-muse-unaligned-vector-move` (assembler rewrites aligned instructions to unaligned, same speed; binutils >= 2.38); gate runs AVX kernels under ASan with default options, `make bench` runs them native on Windows | closed | `make check` (ASan), after predicting only for Windows |
| 20 | 2026-09-17 | error | test «each tier = scalar» passed even with two lanes swapped in AVX2 kernel | special values in 1 element out of 16: almost every lane had infinity or NaN, and one NaN equal to another hides every rounding difference | special values rare and only in odd rounds; `test_kernels` contains wrong variant on purpose (lanes swapped) that comparison must see, or test fails; also tested by hand FMA and wrong bytes | closed | me, deliberately breaking kernels |
| 21 | 2026-09-17 | errore | aggiunto un flag nel `Makefile`, il test sotto ASan andava ancora in crash: gli oggetti erano quelli vecchi | le regole `.o` dipendevano solo dal `.c` e dagli header, non dai flag | ogni `.o` dipende dal `Makefile`: un flag cambiato ricompila tutto | chiusa | io, contando le istruzioni allineate rimaste nell'oggetto |
| 22 | 2026-09-17 | debito | con 11 GB liberi il motore ha caricato 7.4 GB di pesi e solo dopo ha rifiutato la sessione da 0.03 GB (sarebbero rimasti 2.96 GB, minimo 3.12) | il controllo della memoria al caricamento non somma la sessione che verrà creata | stimare pesi + sessione minima prima di leggere il file; test con un modello sintetico e una soglia finta | aperta | io, misurando il modello vero |
| 23 | 2026-09-17 | scoperta | una mutazione di prova (`free(malloc(64))` dentro la generazione) non faceva fallire il test delle allocazioni | a `-O2` gcc toglie una coppia allocazione-rilascio che non serve a niente: la mutazione non allocava davvero | le mutazioni di prova e la sonda del test usano un puntatore `volatile`, che il compilatore non può togliere; `test_hot` verifica prima che il contatore veda una sonda vera | chiusa | io, rompendo apposta il motore |
| 24 | 2026-09-17 | errore | misurato il pool nuovo con un `bench_kernels` vecchio: numeri identici a prima, sembrava che il cambio non servisse | `make test` non ricompila il benchmark, e l'ho lanciato direttamente | il cancello ricompila `bench_kernels` in Linux; *regola*: le misure si lanciano con `make bench` o `make profile`, che ricompilano | aperta: serve che il benchmark stampi il commit o l'ora di compilazione | io, vedendo numeri uguali al millesimo |
| 25 | 2026-09-17 | scoperta | ThreadSanitizer non parte nel container: «unexpected memory mapping» | il kernel di WSL2 randomizza la memoria con più bit di quanti TSan ne supporti | nel cancello i test TSan girano con `setarch -R` e il container con `--security-opt seccomp=unconfined`; TSan su `test_base` e `test_hot` a ogni `make check` | chiusa | io, primo tentativo di TSan sul pool nuovo |
| 26 | 2026-09-17 | scoperta | l'hook contro le barre rovesciate (#18) guarda solo lo strumento Bash: un `python -c "...write('a\nb')"` lanciato dallo strumento PowerShell passa, e il file si rovina allo stesso modo | il matcher dell'hook in `.claude/settings.json` è `"Bash"` | matcher `"Bash\|PowerShell"` (il comando è nello stesso campo `tool_input.command`), applicato col sì di Marcello: da PowerShell la scrittura con `\n` è bloccata, quella senza passa | chiusa | io, verificando l'hook |
| 27 | 2026-09-17 | scoperta | spostare rope, attivazione ed attenzione non ha cambiato un bit dei logit del modello vero (200 token, 16/3/1 thread, scalare contro AVX-512) | operazioni elemento per elemento e somme nello stesso ordine: la divisione in thread non tocca i numeri | `test_kernels`: rope da tabella = rope con `pow`/`cos`/`sin` per elemento, swiglu con 1/2/3/8 thread = seriale, `tr_attention_head` = ciclo di prima, `axpy_f32` di ogni tier = scalare (con una variante FMA che deve risultare diversa); le tre mutazioni di prova sono state prese | chiusa | io, confronto dei logit prima/dopo |
| 28 | 2026-09-17 | errore | la prima stesura del tokenizer teneva il contesto dell'ordinamento degli added token in una variabile statica: due tokenizer caricati insieme da due thread si sarebbero pestati | `qsort` non ha un argomento di contesto, e la scorciatoia è stata una variabile globale | `tools/lint.py` rifiuta ogni variabile statica mutabile in `src/` (puntatori compresi) senza la nota `global-ok: motivo`, dopo aver provato su campioni che il controllo le vede; le 13 esistenti sono annotate come fatti del processo | chiusa | io, rileggendo prima di compilare |
| 29 | 2026-09-17 | errore | il piano del tokenizer citava solo colibri `tok.h`; ds4, che legge il vocabolario dal GGUF come serve a noi, è stato letto solo quando Marcello l'ha chiesto | la colonna «da dove viene l'idea» di `docs/ARCHITECTURE.md` è stata scritta una volta sola, senza confrontare le due implementazioni componente per componente | da fare: una tabella di confronto per componente in `docs/ORIGINS.md` (cosa fa colibri, cosa fa ds4, difetti, cosa si prende, cosa si migliora e con quale prova) e un controllo in `tools/lint.py` che la pretende per ogni cartella di `src/` | aperta | Marcello |
| 30 | 2026-09-17 | scoperta | il tokenizer di OLMoE in transformers normalizza in NFC prima di tokenizzare; llama.cpp no, e nemmeno colibri e ds4: su testo con accenti scomposti (nomi di file da macOS) i token cambiano | `normalizer: NFC` in `tokenizer.json`, che il GGUF non registra | la famiglia `olmo` porta `nfc = 1` nella tabella di `tokenizer.c`; `test_tokenizer` (e + U+0301 = é); l'oracolo confronta il testo normalizzato e fa lo sweep della forma NFD di ogni codepoint | chiusa | io, leggendo `tokenizer.json` |
| 31 | 2026-09-17 | scoperta | il nostro convertitore scriveva un vocabolario diverso da quello dei GGUF veri: tutti gli added token CONTROL, pre "default", niente `[PAD]` | `gguf.BpeVocab` di gguf-py non è la logica di `convert_hf_to_gguf.py` di llama.cpp | `hf_to_gguf.py` rifà `get_vocab_base` di llama.cpp; `make oracle-tokenizer` confronta ogni chiave `tokenizer.*` con il GGUF del modello vero quando c'è (0 differenze) | chiusa | agente, confronto con il GGUF vero |
| 32 | 2026-09-17 | scoperta | `tokenizers` 0.22.2 non usa Unicode 15.1: non scompone U+11938 e la sua tabella delle classi combinanti tratta come 0 112 segni recenti; tabelle prese da Python avrebbero dato token diversi | HF porta tabelle Unicode proprie e più vecchie | `tools/gen_unicode_tables.py` sonda HF, confronta la sua copia dell'algoritmo con HF su ogni codepoint, coppia e 25 000 sequenze, e non scrive se c'è una differenza; l'oracolo fa lo sweep di tutti i codepoint | chiusa | agente, generatore delle tabelle |
| 33 | 2026-09-17 | errore | il tokenizer rifiutava il GGUF vero di OLMoE: «no token for byte 0xc0» | ho dato per scontato che ogni vocabolario byte-level abbia i 256 byte; GPT-NeoX non ha i 13 byte che l'UTF-8 valido non contiene | il byte senza token si scarta prima dei merge, come HF; `test_missing_byte`; l'oracolo sul vocabolario vero gira in `make check` | chiusa | oracolo, primo giro |
| 34 | 2026-09-17 | errore | con `--no-parse-special` 2 testi davano token diversi da HF: IP_ADDRESS seguito subito da EMAIL_ADDRESS, che condividono le barre | saltavo i token di controllo durante la ricerca; HF li trova comunque e ne consuma il testo, così un token utente sovrapposto non si trova | il token di controllo non analizzato consuma il suo testo; `test_tokenizer` con un token utente che sta dentro il testo di un token di controllo; l'oracolo gira anche senza parse-special | chiusa | oracolo, primo giro |
| 35 | 2026-09-17 | errore | la prova delle mutazioni del tokenizer è rimasta appesa 10 minuti | una mutazione creava un pezzo di lunghezza zero e un ciclo infinito, e i test giravano senza limite di tempo | `split_gpt2` fa avanzare ogni pezzo di almeno un codepoint; `make test` lancia ogni test con `timeout 300` | chiusa | io, prova delle mutazioni |
| 36 | 2026-09-17 | errore | il controllo degli accenti da riga di comando falliva dentro `make check` ma passava da Git Bash | GNU make su Windows passa la ricetta alla shell nella code page locale: «città» arrivava rovinato prima del motore | il testo sta in `tools/check_argv_utf8.sh`; `tools/lint.py` rifiuta caratteri non ASCII nelle ricette del Makefile | chiusa | `make check`, primo giro del controllo |
| 37 | 2026-09-17 | errore | `trochilus chat` non partiva sul PC di Marcello: «need 1.00 GiB, only 3.68 GiB available» | la chat chiedeva sempre la cache per tutto il contesto di addestramento (4096 token, 1 GB); con i container Docker degli altri progetti accesi la RAM libera non bastava, e l'utente doveva conoscere `-c` | senza `-c` la chat dimezza il contesto (fino a 512) finché la guardia della memoria accetta, e lo dice; provato occupando RAM apposta (4096 e 2048 rifiutati, 1024 accettato, risposta regolare). Manca un test automatico: arriva con il piano automatico della memoria (M1), che deve poter ricevere un budget finto | aperta | Marcello |
| 38 | 2026-09-17 | errore | dopo le prove pesanti nel container (build di llama.cpp, tre caricamenti del modello) su Windows restavano 2,65 GB di RAM libera | la macchina virtuale di Docker tiene i file letti nella sua cache e non restituisce la memoria a Windows | `make check` su Windows svuota la cache della VM alla fine (`drop_caches`); *regola*: stessa cosa dopo ogni prova pesante lanciata a mano nel container | aperta: le prove a mano non sono coperte da un controllo | io, prova nativa rifiutata dalla guardia della memoria |
| 39 | 2026-09-17 | scoperta | llama.cpp non è un riferimento esatto per i logit: sullo stesso Q8_0 la KL media contro Trochilus è 9e-3 e la generazione greedy si separa dopo 19 token | llama.cpp quantizza a 8 bit anche le attivazioni e tiene la KV in f16 | il riferimento esatto resta transformers: `make oracle-real` (in `make check`) confronta Trochilus con transformers sui pesi veri tagliati a 2 layer, prompt corto e da 1024 token: token greedy identici, logit entro 1e-3 (misurato 2.4e-4) | chiusa | io, `tools/compare_llamacpp.py` |
| 40 | 2026-09-17 | scoperta | la velocità di decode stampata da `trochilus generate` contava N token su N-1 valutazioni: +3% con 32 token, confronto con llama.cpp falsato | il primo token generato viene dai logit del prompt, senza valutazione | `generate` stampa token e valutazioni e divide per le valutazioni; `make check` verifica «4 tokens, 3 evaluations»; i numeri di `make profile` erano già giusti (il profiler conta le valutazioni) | chiusa | io, scrivendo `tools/speed_compare.py` |
| 41 | 2026-09-17 | scoperta | colibri dentro Docker leggeva gli esperti dal bind mount di Windows: 0.7 tok/s invece di 12 | il file system condiviso con Windows (9p) è lento, e colibri legge gli esperti dal disco a ogni cache miss | modelli per le misure in un volume Docker (`trochilus-models`, ext4 nella VM); *regola*: nessuna misura di velocità su file in bind mount | aperta: regola | io, prova rapida |
| 42 | 2026-09-17 | scoperta | nessuna delle tre fonti prova che il prefill a blocchi dia i logit del calcolo token per token: llama.cpp e ds4 quantizzano le attivazioni a 8 bit nel lotto, ds4 non ha test, colibri non lo dichiara | il lotto è trattato come ottimizzazione di velocità, non come lo stesso calcolo | `tests/test_prefill.c` (n_batch, divisione in chiamate, thread, f32/Q8_0, rewind, decode dopo il lotto: `memcmp`); `tools/oracle.py` confronta al byte `logits -b 3/64/tutto` con un token per passata su tiny f32/f16/q8_0 e OLMoE a 2 layer | chiusa | io, mappa delle fonti |
| 43 | 2026-09-17 | scoperta | i test nuovi del prefill sono passati al primo colpo: un test mai visto rosso non prova niente, e il riferimento di `test_prefill` è lo stesso motore con un token per passata (non vede un errore comune ai due percorsi) | test scritto insieme al codice che controlla | due mutazioni su una copia (attenzione non causale nel lotto, logit della prima passata): 72 casi su 90 rossi, i 18 verdi sono quelli da un token per passata; gli errori comuni ai due percorsi li vedono l'oracolo transformers e il `cmp` col binario di prima | aperta: *regola* (mutazioni a mano, non in `make check`) | io, prima di dire fatto |
| 44 | 2026-09-17 | errore | `threads.h` usava `size_t` senza includere `<stddef.h>`: build nativo Windows verde, gcc Linux rotto | MinGW tira dentro `stddef.h` da un altro header, glibc no | nessuna nuova: `make check` compila già con gcc e clang su Linux e l'ha fermato prima di ogni misura | chiusa | il cancello |
| 45 | 2026-09-17 | errore | il kernel «una riga contro 4 token» non guadagnava niente: gli accumulatori erano un array `__m512 acc[4]` e il compilatore li teneva sullo stack (9 operazioni FP diventavano 3 più load/store a ogni giro) | `-O2` non srotola il ciclo sui 4 token, e un accumulatore indicizzato non può stare in un registro | registri con nome, uno per token, e commento sul perché; riga `dot_row q8_0 x4` in `make bench` (44 contro 21 G elementi/s: se torna a 21 l'array è tornato) | chiusa | io, leggendo l'assembly generato |
| 46 | 2026-09-17 | errore | ho concluso «nessun guadagno» da confronti falsati: lo stesso prefill dava 144 tok/s all'inizio della sessione e 120-130 un'ora dopo, con la macchina calda. Due varianti misurate una dopo l'altra (prima tutta A, poi tutta B) si portano dietro quella deriva: il kernel a 4 token sembrava inutile e invece vale 1.45× | portatile che perde 10-25% scaldandosi; mediana di 5 dentro una sola variante non vede la deriva fra varianti | `tools/ab_speed.sh`: run alternate A, B, A, B... e mediane per cella, round 0 scartato; *regola*: nessun confronto fra due binari senza alternarli | aperta: regola, ma lo strumento la rende comoda | io, rimisurando alternato |
| 47 | 2026-09-17 | scoperta | dentro Docker/WSL2 `lscpu` dice una sola L3 da 32 MB per 16 core: la topologia vera del 7940HX è 2 CCD da 32 MB (Windows: `L3CacheSize` 65536 KB). Una prova sul collocamento dei thread fatta nel container non dice niente della macchina | il guest vede vCPU sintetiche, e l'affinità dentro la VM non fissa i core fisici dell'host | *regola*: le prove su CCD, SMT e affinità si fanno native (`docs/MEASUREMENTS.md` §Dove vanno i thread); la correttezza resta in container | aperta: regola | io, confrontando `lscpu` e Windows |
| 48 | 2026-09-17 | scoperta | il prefill rendeva 1.16× da 8 a 16 thread e sembrava il limite di potenza del portatile: è lo scheduler di Windows che appoggia due dei 16 thread sullo stesso core fisico lasciando core liberi. Un thread per core fisico vale +30% (134 → 174 tok/s), e da 8 a 16 core il prefill rende 2.02× | 16 thread su 32 processori logici e nessuna affinità: il sistema è libero di raddoppiare su un core | thread del pool fissati ai core fisici (prossimo passo); fino ad allora *regola*: ogni misura di velocità dichiara il collocamento | aperta: il pin nel pool la chiude | io, misurando clock e affinità |
| 49 | 2026-09-17 | errore | una sonda nuova non trovava mai i tok/s: le righe di tempo di `trochilus` vanno su **stderr**, su stdout c'è solo `tokens:` | `tools/ab_speed.sh` lo sapeva (`2>&1 >/dev/null`), io ho riscritto la cattura da zero | *regola*: chi legge le misure legge stderr; `ab_speed.sh` resta lo strumento buono | aperta: regola | io, sonda che stampava `prefill=?` |
| 50 | 2026-09-17 | errore | il primo `test_spec` era verde sui token senza aver mai provato un rifiuto: il modello sintetico ripete un token solo, quindi ogni bozza veniva accettata (390 accettate, 0 rifiutate). Come #43, un test che non vede il ramo non lo prova | il riferimento è il modello stesso, e un modello che si ripete rende la speculazione sempre giusta | `tests/test_spec.c` gira anche con un vocabolario ristretto (argmax sui primi 8 logit): i token variano e le bozze sbagliano; `TR_CHECK(accepted > 0)` e `TR_CHECK(rejected > 0)` fanno fallire un test che prova metà del codice | chiusa | io, guardando le statistiche del test |
| 51 | 2026-09-17 | scoperta | uno step speculativo emette **più token di quanti ne restano da produrre**: chi conta i token deve troncare l'uscita, e la sessione resta avanti rispetto ai token consegnati | il passo verifica 1 + k posizioni e accetta tutte quelle che il modello avrebbe scelto comunque | `tests/test_spec.c` confronta la storia intera (`g.n_hist`) con un riferimento lungo `N_GEN` più la bozza massima, e i logit finali con la posizione vera; contratto in `src/gen/greedy.h` | chiusa | il test, al primo giro rosso |
| 52 | 2026-09-17 | errore | ho compilato nel container con il `BUILD` di default: gli oggetti Linux hanno sovrascritto quelli nativi in `build/`, e il link successivo avrebbe mescolato due piattaforme | `BUILD := build` è lo stesso su Windows e in Docker, e i `.o` finiscono negli stessi percorsi | `make` scrive `$(BUILD)/.platform` e si ferma se la cartella è di un'altra piattaforma o di un altro compilatore; nel container si passa `BUILD=build/linux-gcc`, come fa `make check-linux` | chiusa | io, guardando cosa avevo appena sovrascritto |
| 53 | 2026-09-17 | scoperta | la speculazione dal prompt non è gratis: con bozza 8 e solo il 13% di token accettati il decode passa da 29.6 a 18.4 tok/s (0.62×). Una riga in più di una passata costa ~5.2 ms contro i ~34 ms della passata, quindi il pareggio è intorno al **15% di bozze accettate** | il costo dei pesi si ammortizza, il lavoro per elemento no: k righe in più sono k volte il lavoro per elemento | misurato e scritto (`docs/MEASUREMENTS.md` §Speculazione dal prompt); `--spec` spento di default; chiusa dalla bozza adattiva (domanda 25, #55), che riporta il caso peggiore a «come senza» | chiusa | io, misurando invece di assumere che fosse gratis |
| 54 | 2026-09-17 | errore | `make spec-check` era verde e non provava niente: girava sul modello tagliato a 2 layer, che scrive testo senza ripetizioni, quindi la bozza non partiva mai (`0/0 drafted`) e confrontava due run senza speculazione. Terza volta che un controllo verde non vede il ramo (#43, #50) | il controllo chiedeva solo che la riga «speculation:» esistesse, non che ci fosse una bozza accettata | `spec-check` gira sul modello **intero** con un prompt che contiene il file da riscrivere, e pretende `[1-9][0-9]*/[1-9][0-9]*` accettate: senza bozze accettate fallisce. Il gate monta il volume `trochilus-models` quando c'è, così il modello si legge da ext4 e non dal bind mount | chiusa | il controllo stesso, appena reso severo |
| 55 | 2026-09-17 | scoperta | il primo test dello shrink adattivo (`k_cur`) usava il vocabolario ristretto della #50 e restava rosso per il motivo sbagliato: su quel percorso `tr_lookup_draft` non trova quasi mai un n-gramma (bozza `k == 0`), che per regola non deve toccare `k_cur` — il test non stava mai esercitando un rifiuto vero (`k > 0`, `a < k`) | il vocabolario ristretto della #50 rende i token imprevedibili, ma imprevedibili vuol dire anche che smettono di ripetersi: niente n-gramma da proporre, non una proposta sbagliata | `tests/test_spec.c` misura shrink e crescita sullo stesso prompt quasi periodico e sullo stesso vocabolario reale (48): 1500 token generati su un contesto di 2048, `k_cur` minimo e massimo tracciati a ogni passo; il minimo scende sotto `n_draft` a ogni vero rifiuto, il massimo torna a `n_draft` ripartendo da 1 | chiusa | io, leggendo la stampa diagnostica del test rosso (`k_cur` fermo al valore iniziale) |
| 56 | 2026-09-17 | errore | `tools/ab_speed.sh` ha stampato la tabella delle mediane **vuota** invece di fermarsi: il motore rifiutava di caricare («not enough memory») e nessuna run produceva la riga tok/s | lo script passava l'output a `sed`: nessuna corrispondenza = nessuna riga, e il riepilogo girava lo stesso su un file vuoto. Una misura che non misura deve rompere, non restare in silenzio | *regola* in codice: `ab_speed.sh` e `ab_spec.sh` si fermano alla prima run senza riga tok/s e stampano le ultime righe di quella run (il motivo vero, in questo caso la memoria) | chiusa | io, aprendo un file di risultati che aveva due righe |
| 57 | 2026-09-17 | scoperta | misurare mentre un agente gira `make check` in Docker dà numeri falsi: il primo A/B del pin dava +2% sul prefill a 16 thread, lo stesso binario a macchina libera dà +17-23% | la VM di WSL aveva preso 10.9 GB (cache del container, #38) e la CPU era occupata dal build: il caso A e il caso B si alternano, ma la contesa non è costante nel tempo | *regola*: nessuna misura di velocità mentre un agente lavora; prima di misurare si svuota la cache della VM (`sync; echo 3 > /proc/sys/vm/drop_caches`, già in fondo a `make check`) e si guarda `docker ps` e la memoria libera. La #56 la rende visibile: se la memoria manca, la misura si ferma invece di mentire | aperta: regola | io, rifacendo la stessa misura a macchina ferma |
| 58 | 2026-09-18 | scoperta | il pin dei thread aveva un modo giusto e uno sbagliato, e la differenza è tutta nel decode: legare ogni thread a **un processore logico** costa il 17-18% sul decode a 16 thread, legarlo al **suo core fisico** (i due fratelli SMT, uno solo per core) lo riporta alla pari e tiene il +24% del prefill. **Corretto dalla rimisura con A/A (#66)**: il 17-18% non si riproduce (−3.4% da nessun pin, −1.6% dal pin al core: non distinguibile); la differenza fra i due modi è nel **prefill** (+9% al core, spread 5% contro 25%). Il default resta il pin al core | quello che serve è impedire che due thread finiscano sullo stesso core, non inchiodare un thread a un processore: il fratello libero è la valvola che il decode usa per la sua parte seriale | default `TR_POOL_PIN=2` (pin al core); i tre modi (0, 1, 2) restano selezionabili e la tabella dei tre casi è in `docs/MEASUREMENTS.md` §Il pin dei thread; `tests/test_base.c` chiede al sistema operativo dove ha girato ogni worker e pretende che due worker non condividano mai un core fisico | chiusa | io, misurando il decode invece di fermarmi al prefill |
| 59 | 2026-09-18 | errore | una riga in più di una passata era stata misurata **in container** (~5.2 ms su 34) e da lì era nata la soglia di pareggio della speculazione al 15% di bozze accettate. Nativo, sul modello vero e col pin, una riga in più costa **15-22 ms** (misurata poi per zona, #67: 13.7-17.6 ms): il pareggio è intorno al 50-65% (per zona: 44-56%), e la bozza adattiva che si limitava ad accorciarsi restava a 0.89× sul prompt in cui il modello inventa | il token in bozza sceglie quasi sempre **altri esperti**, e la passata legge anche i pesi di quelli: nel decode il costo è la lettura dei pesi, non il calcolo, e questo il container (memoria virtualizzata, altra banda) lo nascondeva | la bozza adattiva ora **si ferma del tutto** dopo una bozza tutta sbagliata, con pausa che raddoppia (1, 3, 7, 15); `tests/test_spec.c` lo prova su ogni passo e fallisce se in tutta la suite nessuna bozza è mai stata rifiutata del tutto; *regola*: un numero che dipende dalla memoria non si porta dal container al nativo — si rimisura dove gira il prodotto | chiusa | io, ricavando i ms/passata dai passaggi stampati invece di fidarmi della soglia scritta |
| 60 | 2026-09-18 | errore | la pausa della bozza adattiva, dichiarata «1, 3, 7, 15, capped 16» in `greedy.h`, STATO e MISURE, faceva **1, 3, 7, 15, 31, 16, 16**: la quinta pausa durava 31 passi | il tetto era provato sul valore vecchio (`back < 16 ? back*2+1 : 16`): 15 passa il controllo e raddoppia a 31. Il test della pausa guardava solo il passo subito dopo una bozza sbagliata, mai quanto durava la pausa | `test_adaptive_pause_is_capped` in `tests/test_spec.c` forza ogni bozza a fallire (riscrive la storia che legge il drafter; la scelta vera viene da una sessione ombra) e pretende 1, 3, 7, 15, 16, 16 con i token ancora identici: rosso prima del fix (`31 != 16`), verde dopo. Sui due prompt di banco l'effetto è nullo (65 contro 66 righe di bozza su 200 token, replay esatto) | chiusa | revisione (Fable), leggendo `greedy.c` contro il suo commento |
| 61 | 2026-09-18 | errore | con **due pool vivi insieme**, distrutti nell'ordine in cui erano nati, il thread chiamante restava inchiodato al core 0 per il resto del processo (affinità `[0-31]` prima, `[0,1]` dopo) | ogni pool salvava per sé «dov'era il chiamante»: il secondo salvava il pin del primo e, distrutto per ultimo, rimetteva quello | l'affinità di prima sta nel thread (`t_pin_prev`) con un contatore dei pool vivi (`t_pin_depth`): torna quando muore l'ultimo, in qualunque ordine. `test_pool_caller_affinity_any_order` in `tests/test_base.c` (rosso prima, verde dopo, anche sotto TSan) | chiusa | revisione (Fable), programma di prova sul pool |
| 62 | 2026-09-18 | errore | i thread oltre l'ultimo slot, che il commento di `threads.c` diceva «lasciati allo scheduler», su Linux finivano **tutti sul core del chiamante**: processo ristretto a 2 processori, pool da 4, i worker 2 e 3 con affinità `[0]` invece di `[0,2]` | su Linux un thread nuovo eredita l'affinità di chi lo crea, e il chiamante si era appena fissato allo slot 0. Nessun test creava più thread che slot | il worker senza slot riprende l'affinità che il chiamante aveva prima del pin (`worker_start`); `test_pool_oversubscribed` rilancia `test_base --oversubscribed` in un processo ristretto a 2 processori (`tr_cpu()` si rileva una volta sola) e legge l'affinità di ogni worker: rosso prima, verde dopo | chiusa | revisione (Fable), programma di prova sul pool |
| 63 | 2026-09-18 | debito | due cose del pool che reggono solo finché il processo ha **un** pool: (a) due pool vivi prendono gli stessi slot dal primo in giù (worker 1 di entrambi su `[2,3]`): usati insieme si dividono gli stessi core e lasciano fermi gli altri, cioè il caso «32 thread SMT» di MISURE (decode -90%); (b) un `tr_parallel_for` su un pool B fatto da dentro un body del pool A gira seriale ma riceve l'id del worker di A: pool B da 2, id visto **3**, fuori da `[0, tr_pool_size)`, e `olmoe.c` indicizza con quell'id le righe degli score di attenzione | gli slot sono un fatto del processo ma ogni pool li assegna come se fosse solo; l'id del worker è del thread, non del pool | *regola*, scritta in `threads.h`: un pool alla volta, e da un body si annida solo sul proprio pool. La soluzione (slot presi da un registro di processo, id per pool) è una scelta di progetto e va fatta quando arriva il secondo modello nello stesso processo; la prova è il programma di prova della revisione (uscita in `docs/MEASUREMENTS.md` §Revisione) | aperta: regola | revisione (Fable), programma di prova sul pool |
| 64 | 2026-09-18 | errore | il cancello non ha mai fatto girare il **motore** sotto un tier che non fosse il migliore della macchina: `TR_CPU_MAX` non compariva nel `Makefile`, e `test_base.c` diceva «verificato a mano». In più un valore sbagliato (`TR_CPU_MAX=AVX2`, `bogus`) veniva ignorato in silenzio: chi crede di provare avx2 prova avx512 | `test_kernels` confronta i kernel di ogni tier con lo scalare, ma prefill, speculazione e sessione giravano solo col tier scelto da `tr_kernels_init` | `make tier-check` (`tools/tier_check.sh`, dentro `check-linux`): prima prova che il tetto ha davvero tagliato, poi `test_prefill`, `test_spec`, `test_session` sotto `scalar` e `avx2`, poi i logit dei modelli minuscoli f32/f16/q8_0 identici al byte fra tier, thread e `-b`. Provato per mutazione: col tetto rotto lo script fallisce. Un valore sconosciuto ora è un avviso sul log. Risultato della prima run: tutto identico, l'invariante regge | chiusa | revisione (Fable), cercando `TR_CPU_MAX` nel Makefile |
| 65 | 2026-09-18 | errore | «la leva 2 (int8/VNNI) non si scrive: +1-8%» era una conclusione più debole della sua misura: il candidato era una riga contro **un** token, a 256 bit, con half→float scalare e una catena scalare di correzione dentro il ciclo, confrontato col nostro dot a un token; ma il prefill gira col kernel a **4** token, e con quella struttura l'int8 non era mai stato misurato. Rimisurato, run alternate, gcc e clang: **VNNI x4 a 512 bit 1.8-2.3×** il nostro x4 float (256 bit: 1.45-1.96×), mentre la strada «esatta» indicata come prossimo passo (una riga contro **8** token) dà **1.13× a n=1024, 0.79× a 2048, 0.93× a 4096**: a 2048 le attivazioni float di 8 righe non stanno più in L1 | si è confrontata la struttura sbagliata (mele con pere), e il passo successivo è stato scelto senza misurarlo | i tre candidati stanno in `tests/bench_kernels.c` (sezione «second take»), misurati alternati col kernel del motore e con un controllo che calcolino davvero il loro dot (x8 identico al bit a x4; int8 contro il dot int8 in C puro): `make bench` rifà il numero. *regola*: un candidato si confronta con il kernel che gira davvero, con la sua stessa struttura. La decisione resta di Marcello: l'int8 non è esatto, può essere solo un modo esplicito | chiusa la misura, aperta la decisione | revisione (Fable), riscrivendo il candidato |
| 66 | 2026-09-18 | scoperta | il metodo delle misure native ha due buchi: `ab_speed.sh`/`ab_spec.sh` fanno girare sempre A e poi B (B trova sempre la macchina scaldata da A: l'effetto d'ordine non si cancella), e nessuna sessione ha un controllo **A/A**, quindi non si sa sotto quale differenza due modi sono «uguali». Conclusioni tirate su 3 giri dentro lo spread: decode 8 contro 16 thread (31.0 contro 28.7, intervalli 28.4-31.5 e 26.4-29.8 sovrapposti), pin al core contro pin al processore (212.5 contro 178.6 diretto, ma 1.24× e 1.21× contro «nessun pin» in altre due sessioni, cioè 1.02× indiretto), caso peggiore della speculazione 1.01× (il replay esatto dei passaggi dà 0.94-1.00× per un costo di riga di 15-22 ms). **Rimisurate** (`tools/remeasure.sh`: nativo, macchina ferma, 8 giri, A/A): una era **sbagliata** (caso peggiore **0.953×**, non 1.01×: sotto `--spec 0` in 8 giri su 8, intervalli disgiunti), una **non si riproduce** (il −17% sul decode del pin al processore è −3.4% da nessun pin e −1.6% dal pin al core, non distinguibile: la differenza fra i due pin è nel prefill, +9%), una è **confermata** (decode a 8 thread 1.09-1.12× su 16, intervalli disgiunti). E il controllo A/A è rumoroso anche lui: lo stesso decode a 16 thread dà 0.5% in un blocco e 2.2% in un altro | mediana di 3 con min-max non dice se due intervalli che si toccano sono diversi; e i confronti fra sessioni diverse portano la deriva della macchina (#46) | `tools/ab_modes.sh`: modi dello stesso binario (variabile d'ambiente o flag), il primo modo del giro ruota, un modo dato due volte fa da controllo A/A, mediana min max spread e rapporto. `tools/remeasure.sh` rifà le tre misure e le zone con un comando (ferma e riavvia i container). *regola*: la soglia è il **peggiore A/A della sessione per quella fase**, la differenza deve superarla contro **tutte e due** le copie del controllo, e sotto si scrive «non distinguibile» (`docs/STATUS.md` §Decisioni, `docs/MEASUREMENTS.md` §Revisione) | aperta: regola (misure rifatte il 2026-09-18; il verdetto contro l'A/A lo legge ancora chi guarda la tabella: `ab_modes.sh` non lo stampa) | revisione (Fable), confrontando fra loro le tabelle di MISURE |
| 67 | 2026-09-18 | scoperta | domanda 12, la metà che non vuole cronometro: i contatori del profiler (byte di pesi per passata, deterministici, modello vero, 200 token) dicono che una riga di bozza fa leggere **2.3-4.7 esperti nuovi per layer su 8**, non 8: la prima riga in più aggiunge 445-475 MiB ai 1200 di una passata, a bozza 8 ne aggiunge 252 a testa, a 15 ne aggiunge 179. La spiegazione di #59 («il token in bozza sceglie altri esperti») è vera a metà: l'altra metà del costo è il calcolo della riga, che cresce anche nelle zone dense dove non c'è nessun peso nuovo da leggere (4-5.6 ms per riga in container). **La metà a tempo, nativa a zone** (`tools/remeasure.sh`): una riga in più costa **17.6 ms** se è la sola e **13.7** l'una se sono otto, contro 31.3 di una passata; negli esperti sta il 91% e il 61%, e lì il tempo segue i MiB di esperti nuovi (30 MiB/ms ai due punti); le zone dense sono gratis per la prima riga in più (il kernel a 4 token) e valgono 4.1 ms per riga a nove. «Metà lettura, metà calcolo» vale per le bozze lunghe: per le corte è quasi tutta lettura di esperti. Il replay con questi costi dà 0.95-0.96× sul caso peggiore, il cronometro 0.953×. E il replay esatto delle politiche (`passaggi × 35 ms + righe × C`, contatori del motore riprodotti alla riga) dice che le costanti della pausa spostano ±2-3%: non sono un parametro sensibile | il costo era stato ricavato dall'aritmetica dei tok/s e la spiegazione non era mai stata contata | numeri e metodo in `docs/MEASUREMENTS.md` §Revisione; `tools/remeasure.sh` (blocco 5) rifà la misura a zone con un comando; domanda 12 chiusa | chiusa | revisione (Fable), contando i byte invece dei millisecondi |
| 68 | 2026-09-18 | scoperta | tre commenti dicevano il contrario del codice: `lookup.h` «un match non si sovrappone mai alla coda» (si sovrappone, e `test_lookup` lo usa: `0 0 0 0` propone 0), `threads.h` «ogni thread fissato a un processore logico suo» (il default è il core fisico intero), `threads.c` «i thread in più sono lasciati allo scheduler» (#62) | il commento è rimasto alla prima versione del codice | corretti i tre commenti sul codice e sui test che li provano; *regola*: quando cambia un comportamento si rilegge il commento d'intestazione della funzione | aperta: regola | revisione (Fable), leggendo le intestazioni contro i test |
| 69 | 2026-09-18 | errore | ho modificato `tools/ab_modes.sh` (una riga di `sed` in più) mentre la misura in background lo stava eseguendo: il blocco a contesto 2048 ha fatto un giro oltre l'ultimo, alla run successiva non ha più riconosciuto le righe tok/s e si è fermato senza la tabella delle mediane | la shell legge lo script mentre lo esegue: il testo inserito ha spostato tutto quello che veniva dopo sotto il punto di lettura. Le 45 run erano intatte nel file dei risultati (ogni etichetta una volta per giro e per fase, verificato) e le mediane sono state ricalcolate con lo stesso `awk` | ogni `tools/*.sh` ha il corpo dentro `main() { ... }` chiamata dall'ultima riga (`main "$@"; exit`): la shell lo analizza tutto prima di eseguirne una riga e non rilegge il file dopo; `tools/lint.py` (`check_shell_scripts_whole`) lo pretende per ogni script | chiusa | io, leggendo il log della misura |
| 70 | 2026-09-18 | scoperta | quanti thread vuole il decode **dipende dal contesto**: a 512 token 4 thread sono i più veloci (34.4 tok/s, +1.8% su 8, +11.8% su 16), a 2048 perdono il 3.6% su 8 e non si distinguono da 16; 12 non rende a nessuno dei due. La domanda 26 chiedeva «4 e 12 a contesto 512»: misurata solo lì avrebbe dato 4 | il decode è limitato dalla lettura dei pesi e 4 thread riempiono già il bus; col contesto cresce l'attenzione, dove i thread in più tornano a servire (corretto il 2026-09-19, #75-#76: l'attenzione non è calcolo ma lettura della KV, che andava piano per il layout; il bus si riempie a ~54 GB/s, non a 41) | `tools/threads_phase.sh sweep` misura i due contesti con un comando; il motore non porta il numero scritto: ogni sessione misura la sua larghezza e **rimisura ogni 1024 token**, e `tests/test_phase.c` prova che lo fa (rosso senza la rimisura, per mutazione) | chiusa | io, aggiungendo il blocco a 2048 alla misura chiesta |
| 71 | 2026-09-18 | errore | la prima regola di scelta della larghezza del decode («la più ampia entro il 3% dalla più veloce») a contesto 2048 teneva **16 thread in 10 run su 16**: nessun guadagno (0.996× e 1.012×) dove 8 thread forzati danno 1.05×. A contesto 512, dove avevo guardato, sceglieva 8 in 15 run su 16 e sembrava a posto | il margine era scelto a ragionamento («a parità meglio più thread») senza sapere quanto distano le larghezze: a 2048 16 sta al 2.5-4% da 8, dentro il margine più il rumore del «migliore di tre passate» (2% circa) | `tools/ab_modes.sh` raccoglie la colonna `width` (la larghezza che ogni run si è misurata): la stabilità della scelta si legge nella tabella, a ogni contesto. Margine portato all'1% contando le scelte (16 in 10, poi 5, poi **2** run su 16 a 2048; decode 1.027× e 1.019×), e rimisura ogni 1024 token. `tests/test_phase.c` prova la scelta sui tempi di questa macchina ai due contesti. Resta che a 2048 il default prende metà del guadagno di `--decode-threads 8`: stimatore più robusto, domanda 31 | superata da #88: il margine dell'1% era stato scelto sulla macchina coi quattro core presi (#84), e sta sotto il rumore delle passate; lo stimatore va riscritto (domanda 31) | io, leggendo la colonna `width` del blocco a 2048 |
| 72 | 2026-09-18 | errore | una misura lanciata subito dopo `make check` si è fermata alla prima run: «not enough memory: need 6.85 GiB, only 6.91 GiB available». Lo script aveva svuotato la cache della VM e fermato i container, ma Windows teneva ancora 10 GB in «Memory Compression»; cinque minuti dopo i GiB liberi erano 18 | #38 e #57 dicevano di svuotare la cache e di «guardare la memoria libera»: la prima metà era nello script, la seconda era una frase. Windows si riprende la memoria della VM con minuti di ritardo | `tools/threads_phase.sh` e `tools/remeasure.sh` aspettano che il motore stesso veda 12 GiB disponibili (`trochilus cpu`, un controllo ogni 30 s, al massimo 15 minuti) prima della prima run; il fermo alla prima run senza numero (#56) ha funzionato e ha riavviato i container | chiusa | lo script, fermandosi; io, leggendo il log |
| 73 | 2026-09-19 | errore | sette minuti dopo l'inizio di una misura a macchina ferma **un'altra finestra di Claude** (OpenEMR, di Marcello) ha riacceso sei container che lo script aveva fermato: 95% di CPU e 6.4 GiB. Le run sono andate avanti sporche per altri undici minuti (decode a contesto 512: 17.6 tok/s in un giro, 29.6 in quello dopo), finché la memoria è finita e la guardia del motore ha fermato tutto. 18 minuti di misura buttati | lo script fermava i container all'inizio e non guardava più: «macchina ferma» era vero solo al primo secondo | `tools/ab_modes.sh` esegue `AB_GUARD` prima di **ogni** run e si ferma se fallisce; `decode_context.sh`, `threads_phase.sh` e `remeasure.sh` lo impostano a «nessun container acceso» dopo averli fermati, e `decode_context.sh` lo ricontrolla attorno a `bench_mem` e ai profili (provato: guardia falsa → uscita 3 prima della prima run). Non vede il lavoro dell'altra finestra **senza** container, che alza lo spread (9-29% contro 4-16% nella sessione dopo): le misure lunghe si lanciano con le altre finestre ferme, *regola* | chiusa per i container; aperta per il resto: regola | la guardia di memoria del motore; io, con `docker events` e l'elenco dei processi |
| 74 | 2026-09-19 | errore | lo stadio `exact` di `decode_context.sh` confrontava i logit del binario di prima con quelli del nuovo a passate da 64 e li trovava diversi al primo byte: avevo confrontato due file di **forma diversa** (`logits -b k` scrive una riga per passata, non una per posizione). Il motore era giusto, il controllo no; la finestra di misura si è fermata da sola dopo 4 minuti | ho usato `-b` di `logits` come quello di `generate` senza rileggerne l'uso | `exact` confronta prima contro dopo **a parità di forma** (un token per passata, e passate da 64 su 8 thread) e poi l'ultima riga delle due forme, che è la stessa posizione; fa parte dello script, quindi gira a ogni misura prima/dopo e non parte niente se i due binari non danno gli stessi byte | chiusa | lo script, fermandosi prima di misurare |
| 75 | 2026-09-19 | scoperta | il decode a contesto lungo era lento per **come** la KV stava in memoria, non per quanti byte erano: con `[layer][posizione][testa]` una testa leggeva 512 byte ogni 8 KiB, una pagina nuova a ogni posizione, e passavano 32-35 GB/s dove la RAM ne dà 54; gli stessi byte con le posizioni di una testa in fila passano a 47-48. Decode 1.15-1.18× a contesto 4000, prefill 1.39-1.43×, senza cambiare un bit | il layout era quello naturale per scrivere (una riga per token) e nessuno strumento guardava la banda **per zona**: il totale del token (41-47 GB/s) sembrava «vicino al tetto» | il profiler conta i byte letti per zona e stampa i GB/s di ogni zona (`tests/test_prof.c`, `tests/test_model_prof.c`); `make bench-mem` misura lo stesso kernel sui due layout; `tests/test_kv.c` fissa il layout (5 mutazioni su 5 viste dai test) | chiusa | io, col microbenchmark prima del profilo |
| 76 | 2026-09-19 | errore | per due giorni «41 GB/s» è stato scritto come il tetto della RAM (STATO, MISURE, LESSONS #70: «4 thread riempiono già il bus») e l'ipotesi sul calo col contesto ci era costruita sopra. Era la velocità del **motore** il 17/09; la RAM ne dà ~54, e il decode di oggi ne muove 48-51. La formula a 41 GB/s tornava a contesto 4000 per caso (troppo bassa sui pesi, troppo alta sulla KV) | un limite ricavato dalla cosa che si vuole misurare: nessun microbenchmark della memoria, la domanda 4 era aperta dal primo giorno | `tests/bench_mem.c` (`make bench-mem`): letture in fila e sparse a 1-16 thread, il matmul del motore, l'attenzione; *regola*: un tetto si scrive solo con accanto lo strumento che l'ha misurato | aperta: regola | io, misurando la domanda 4 |
| 77 | 2026-09-19 | scoperta | l'ipotesi sull'attenzione del prompt («quasi tutto lettura ripetuta: ogni token rilegge le stesse chiavi e gli stessi valori») era vera **a metà**: la lettura ripetuta vale fra l'1% e il 21-39% della zona a 4000 token secondo la run (64 MB di chiavi e valori contro 64 MB di L3: sul bordo) e **niente** a 512 e a 2048, dove chiavi e valori di una testa stanno nelle cache. Il grosso, a ogni lunghezza, è il **softmax** (51% a 4000, 67% a 2048, 72% a 512), cioè `expf` della libreria C: 31-33 ns a chiamata con MinGW (istruzioni x87), 4-6 ns con glibc nel container. Lo stesso `expf` fa il 5-7% del prefill nell'attivazione degli esperti | il profiler vede una zona, non le sue fasi: «242 GB/s letti» faceva pensare alla memoria, e nessuno strumento separava prodotto, softmax e somma pesata | `make bench-attn` (`tests/bench_attn.c`): la zona smontata, ogni fase da sola, una query alla volta contro un gruppo per blocco di chiavi, con e senza softmax, e il controllo dei bit; `tests/bench_expf.c` dice quanto costa `expf` e se è arrotondato correttamente. *Regola*: prima di scrivere una leva su una zona, la si smonta in fasi nel banco | aperta: regola; `expf` nostro lo decide Marcello (MISURE domanda 37) | io, col microbenchmark prima del codice |
| 78 | 2026-09-19 | errore | le righe di pesi **F32** (la matrice del router, in ogni GGUF) e **F16** giravano sul ciclo scalare **in ogni tier**: le tabelle AVX2 e AVX-512 partono da una copia della scalare e sostituivano solo le righe Q8_0. Stessi numeri, quindi ogni test era verde: tier contro scalare, logit identici fra tier, oracoli. La zona `router` valeva il 3.6% del prefill a 0.11 GB/s, e un F16 andava 49 volte più piano del dovuto. **Quarta volta che un controllo verde non vede il ramo** (#43, #50, #54): qui il ramo era il dispatch stesso | logit identici fra tier dicono che i tier sono d'accordo, non che un tier venga usato: una variante che manca non è un errore di risultato, e la bit-identità, il solo controllo dei kernel, non la può vedere | `tests/test_tier_used.c`, in `make test` e sotto ogni tier in `make tier-check`. **La tabella**: per ogni tier, ogni voce della zona calda è una funzione sua e non la scalare, per **ogni tipo** di peso che il motore accetta (un tipo nuovo entra coi suoi kernel, o il test è rosso). **Il motore**: un modello per tipo (F32, F16, Q8_0, col router F32 accanto come in ogni GGUF) gira con la tabella attiva avvolta in contatori, e i prodotti contati per tipo devono essere **esattamente** righe × token secondo le forme del modello: una matrice che prende un'altra strada lascia il conto corto. Rosso prima (F16 scalare in avx2 e avx512), verde dopo i kernel F16 (`vcvtph2ps`, bit-identici); due mutazioni (la tabella che perde le righe F32, il matmul che prende la tabella scalare) le vede **solo** questo test, tutti gli altri restano verdi. Regola generale in `CLAUDE.md`: un test si vede rosso almeno una volta, e dice quale ramo esercita | chiusa | io, leggendo i GB/s della zona nel profilo e lo scenario a un thread; il caso F16 l'ha trovato il controllo nuovo |
| 79 | 2026-09-19 | debito | un percorso parallelo **mai diviso in nessun test**: la somma degli esperti (`mix_body`) si divideva sul pool da `1 + 65536 / (n_used × n_embd)` token in su, cioè da 342 token coi modelli dei test, che ne hanno 45. Le zone per token nuove (norme, RoPE, scrittura della KV, router, righe per gli esperti) avrebbero avuto lo stesso buco: il codice parallelo girava solo sul modello vero | soglie in elementi pensate sul modello vero e mai guardate sui modelli minuscoli; `test_prefill` e `test_hot` avevano passate troppo corte | una regola sola in token (`OLMOE_TOKENS_PER_CHUNK` 8) per tutto il lavoro di un token; `test_prefill` con un prompt da 141 token (passate fino a 141, più blocchi e più gruppi di attenzione), `test_hot` con una passata da 20 sotto ThreadSanitizer; `tools/mutate_prefill.sh`: 20 mutazioni su 20 viste (due sono corse critiche e le vede solo TSan) | chiusa | io, scegliendo le soglie delle zone nuove |
| 80 | 2026-09-19 | errore | una mutazione su 17 **non era vista** da nessun test: `tr_attention_group` che calcola un punteggio in più per query (una posizione oltre il suo contesto). Il risultato non cambia, perché softmax e somma pesata usano la lunghezza giusta, ma il kernel legge una chiave oltre il contesto e scrive oltre la riga di punteggi che il chiamante gli ha dato | il test confrontava uscite e punteggi **fino** alla lunghezza attesa, non quello che sta dopo | `test_attention_group` riempie le righe di punteggi con una sentinella e pretende che dopo l'ultima posizione della query non cambi un float; mutazione rifatta: rossa | chiusa | `tools/mutate_prefill.sh` |
| 81 | 2026-09-19 | scoperta | Smart App Control ha tenuto bloccato il binario nuovo per **più di due ore** (le altre volte 10-26 minuti, #12). Il registro eventi di Windows (`Microsoft-Windows-CodeIntegrity/Operational`, evento 3118) dice perché: il verdetto sull'hash resta in cache come «blocca» e il cloud viene richiamato solo ogni due ore circa (05:54, 07:56), con lo stesso esito; i 77 tentativi nel mezzo non hanno chiesto niente a nessuno. Una **seconda copia** compilata in un'altra cartella (`make BUILD=build/after2`), stesso sorgente e altro hash, è partita al primo colpo | «aspetta e riprova, non ricompilare» era pensata per i blocchi brevi, dove ricompilare fa solo ripartire l'attesa; aspettare un hash che il cloud ha già giudicato non lo sblocca | *regola*: un binario bloccato si aspetta mezz'ora; poi, **senza toccarlo** (la sua attesa non riparte), se ne compila una seconda copia in un'altra cartella e si misura con la prima delle due che parte (`TROCHILUS=<binario> sh tools/prefill_context.sh ...`), con gli hash scritti accanto ai risultati (`build/prefill_context/binaries.sha256`). Il primo binario è poi passato da solo, quattro ore dopo la build | aperta: regola | io, leggendo il registro eventi dopo due ore di attesa |
| 82 | 2026-09-19 | errore | una misura è rimasta **sospesa due ore** a metà dello stadio `exact`: dopo tre ore senza input Windows è andato in standby moderno (07:49; processi fermi dalle 08:11 alle 10:11, sveglia dal tasto di accensione). Al risveglio Docker ha riacceso 6 container da solo, e un'istanza **vecchia** dello script, che credevo fermata, ha trovato il suo binario sbloccato e ha cominciato a fermare container e ad aspettare la RAM **accanto** all'istanza nuova: fermare un task in background uccide la shell che l'ha lanciato, non lo script figlio. Vista nel log (righe di due script mescolate) e uccisa a mano prima che misurasse; la sessione di velocità è partita pulita (un solo script, zero container) | nessuno script di misura chiedeva a Windows di restare sveglio, e niente impediva a due misure di girare insieme | `tools/measure_guard.lib`, usato da `prefill_context.sh`, `decode_context.sh`, `threads_phase.sh` e `remeasure.sh`: un lock (`build/.measuring.lock`, col pid: una seconda misura si rifiuta di partire, uscita 5; il lock di uno script morto si riprende) e `tools/stay_awake.ps1`, che tiene sveglia la macchina finché il lock esiste; una misura si ferma uccidendo il pid scritto nel lock (ma il segnale restava in coda finché il figlio in primo piano non finiva, e il trap non usciva: la causa vera e la correzione sono in #84) | chiusa, completata da #84 | io, leggendo il log al risveglio |
| 83 | 2026-09-19 | debito | le costanti di `tr_expf` (i 64 valori di 2^(j/64), ln2/64 in due pezzi, le soglie, le 8 eccezioni coi loro valori a 200 bit) stanno in un header **generato** (`src/kernels/expf_table.h`, da `tools/gen_expf_table.py`); niente impediva di cambiarne una a mano o di cambiare lo script senza rieseguirlo, e **la prova esaustiva non basta a vederlo**: tre costanti spostate di un'unità sull'ultima cifra (2^(1/64), 1/6, il pezzo basso di ln2/64) non cambiano nessun risultato su 4 miliardi di float, quindi `make bench-expf` e `test_expf` restano verdi | un file generato e il suo generatore sono due copie della stessa informazione, e nessuno le confrontava (vale anche per `unicode_data.h`, che però per rigenerarsi chiede `tokenizers`) | `tools/lint.py` rigenera l'header in memoria (`gen_expf_table.py --check`, un secondo) e lo confronta col file, in `make check`; in `tools/mutate_expf.sh` quelle tre mutazioni le vede **solo** questo controllo (17 mutazioni su 17 viste in tutto). Lo script ritrova da sé le eccezioni (`--scan`: mirror numpy bit a bit della strada veloce su tutti i float in campo, le stesse 8 del C su Windows e su Linux) | chiusa | io, scrivendo il generatore; le tre mutazioni invisibili le ha trovate `tools/mutate_expf.sh` |
| 84 | 2026-09-19 | errore | **una prova del 17/09 ha lasciato quattro processi che hanno girato sotto due giorni di misure.** La prova «macchina occupata» del pin (MISURE §Il pin dei thread) era quattro `yes > /dev/null &` scritti a mano: la shell che li aveva lanciati è finita, loro no. Sono rimasti al 100% di un core l'uno dal 2026-09-17 23:59:40 al 2026-09-19 13:11 (26.2 ore di CPU ciascuno, il resto standby), sotto **ogni misura nativa del 18 e del 19/09**: la rimisura con A/A, i thread per fase, il decode a contesto lungo e la banda della RAM, il prefill su prompt lunghi. Su quella macchina è stato scelto il default del motore («il decode vuole 8 thread», domanda 26) e tarato il margine dello stimatore (#88). Rimisurato a macchina pulita (MISURE §Rimisura a macchina pulita): il verso regge (pochi thread), la misura no. Decode a 512 forzato a 8 contro 16 thread: **1.10× coi `yes`, 1.00-1.02× senza** (A/A 1%); banda della RAM 57 GB/s con 4-6 thread, non 54, e 52 con due thread, non 43; numeri assoluti del 3-10% più alti. Trovati chiedendosi perché una sessione avesse spread del 24-57%. Scrivendo la pulizia è uscita la **causa vera di #82**: una shell che aspetta un figlio in primo piano **non serve i segnali** finché il figlio non finisce (bash: «the trap will not be executed until the command completes»), e il trap vecchio (`trap restart EXIT INT TERM`) dopo il segnale faceva proseguire lo script: una misura «fermata» dal pid del lock andava avanti per i venti minuti della sua sessione `ab_modes`, e uccidere script e figli elencati lasciava comunque orfano il motore partito nel frattempo (visto il 19/09 alle 15:57, col percorso vecchio) | **gli script non pulivano ciò che lanciavano**, e niente guardava la macchina prima di misurare: «macchina ferma» era una frase, non un controllo. La guardia sulla CPU (#85) è un cerotto: la causa è il processo lasciato indietro | tre pezzi, ognuno visto rosso e verde. (1) **Ogni script finisce ciò che ha lanciato**: `tools/cleanup.lib`, caricata da ogni `tools/*.sh` (lo pretende `tools/lint.py`, con campioni), uccide dal trap EXIT tutti i discendenti trovati per parentela nella tabella dei processi, su Windows con tutto l'albero nativo (`taskkill /T`: il motore sotto Python non è nella tabella di MSYS); INT e TERM diventano `exit 130`; i passi lunghi girano con `cleanup_run` (in sottofondo più `wait`, che un segnale interrompe subito). `tools/test_cleanup.sh`, in `make check`: uno script vittima con un programma nativo sotto una shell sotto di sé riceve TERM; col trap non resta niente, **senza il trap il figlio deve restare** (la metà rossa gira a ogni esecuzione), e con una `cleanup_children` che non fa niente il test è rosso («with the trap 1 child is still running»). Dal vivo: TERM al pid del lock alle 16:06:59, lock sparito alle 16:07:11, nessun orfano. (2) **`tools/orphans.sh`**: ogni `yes`, ogni programma che gira da `build/`, ogni script di misura, ogni container `trochilus-dev` rimasto; `make check` (bersaglio `clean-machine`, prima di tutto) e `measure_begin` si rifiutano di partire e dicono cosa c'è. Visto rosso con un motore staccato e un `yes` (pid e ora di avvio nel messaggio), verde dopo. (3) **`tools/busy_machine.sh <n> <comando>`**: il solo modo di caricare la macchina apposta; i generatori di carico sono figli suoi, uccisi dal trap, e lo script fallisce se `orphans.sh` ne trova uno. Dal vivo: tre generatori accanto alla guardia, la guardia rossa coi loro nomi, nessun orfano dopo. *Regola*: le conclusioni si tirano solo da sessioni il cui carico di fondo dichiarato (#85) è basso | chiusa (i controlli); le conclusioni del 18-19/09 rimisurate: STATO e MISURE §Rimisura a macchina pulita | io, cercando la causa di uno spread del 24-57%; la causa di #82 scrivendo la pulizia; Marcello ha chiesto che fosse un caso a sé e che la prevenzione fosse la pulizia, non la guardia |
| 85 | 2026-09-19 | scoperta | **le misure native hanno un carico di fondo, e va dichiarato.** A macchina libera (container fermi, nessuna misura, nessuna finestra al lavoro) restano occupati **1.0-2.5 processori logici su 32**, e **0.7-0.9 sono del processo `System` del kernel, sempre**: 0.83-0.88 alle 13:20, 0.77-0.81 su tre finestre da 60 s alle 16:00, 0.68-0.78 nelle sessioni della sera (poi browser 0.04-0.5, antivirus 0.02-0.18, VM di Docker 0.11 quando è accesa). È un fatto di questa macchina: 0.8 core sono il 5% della CPU, e su una fase a 16 thread fissati ai core il passo lo dà il thread più lento, quindi l'effetto può essere più del 5% (non misurato). Quale driver ci sia dentro `System` non si vede senza strumenti da amministratore. Di giorno, con Marcello alla macchina, un'altra finestra al lavoro e l'indicizzatore di Windows, le sessioni hanno spread del 15-30% e non distinguono differenze sotto il 5%; la sera, a macchina lasciata sola, l'A/A torna all'1-3% | il carico di fondo non era mai stato guardato: i numeri di due giorni avevano sotto anche quattro core presi (#84) e nessun log lo diceva | ogni script di misura scrive nel suo log, prima della prima run e dopo l'ultima, «background load: X logical processors busy over 20 s, Y of them the kernel's System process (then: …)» (`tools/background_load.ps1`, contatori grezzi per processo: vede anche `System`, l'antivirus e la VM, che `Get-Process` non legge); prima della sessione `measure_still` aspetta fino a 15 minuti che i processori occupati su 10 s siano al più 3.0, e prima di **ogni run** `tools/machine_still.sh` (in `MEASURE_AB_GUARD`) che siano al più 3.5 su 2 s, poi si ferma e scrive chi tiene la CPU. Visto verde a macchina quieta e rosso con tre `yes` (i nomi nel log); dal vivo alle 16:05 ha tenuto ferma una catena con 3.1-4.7 occupati (`python` di un'altra finestra 1.0, `svchost` 1.2) ed è ripartita a 2.7. Non si corregge: si dichiara, e le misure che decidono si fanno di notte | chiusa (la dichiarazione); aperta: chi c'è dentro `System` (serve una sessione da amministratore) | io, misurando dopo #84; Marcello ha chiesto che fosse un numero nel log e non un sospetto |
| 86 | 2026-09-19 | errore evitato | `build/trochilus-before.exe` **esisteva già**: era il binario di prima della sessione precedente, quello il cui hash sta accanto ai risultati di quella misura. L'istruzione «copia il binario in `trochilus-before.exe`» l'avrebbe sovrascritto | un nome generico per «il binario di prima» si riusa da una sessione all'altra; un hash no | il vecchio è stato tenuto come `build/trochilus-before-prefill.exe`; `tools/prefill_context.sh` scrive da solo `build/prefill_context/binaries.sha256` all'avvio (prima lo si scriveva a mano), e le cartelle dei risultati delle sessioni rifatte si tengono da parte (`build/*-with-yes`, `build/prefill_context-expf-noisy`). *Regola*: prima di sovrascrivere un binario di riferimento si guarda cosa c'è | aperta: regola (gli hash sono automatici) | io, guardando la cartella prima di copiare |
| 87 | 2026-09-19 | scoperta | **la VM di Docker può ripartire da sola**, e con lei ogni container con una politica di riavvio: alle 15:57:33 la VM è ripartita (uptime 351 s alle 16:03) e i sei container di OpenEMR, fermati a mano due ore prima, si sono riaccesi da soli; `colibri-dev` e `redis-dev` no. È lo stesso schema di #84: qualcosa che riparte senza che nessuno lo voglia e finisce sotto una misura. È successo tre secondi dopo la prima chiamata di `orphans.sh` a `docker ps --filter ancestor=…` dopo ore senza container, mentre le centinaia di `docker ps -q` della guardia non l'avevano mai fatto; e di nuovo alle 18:40:46, al primo `docker run` dopo due ore e mezza di misure senza container (i sei di OpenEMR erano di nuovo accesi da soli). **Visto due volte**: Docker Desktop ferma la VM quando non gira nessun container, a `docker ps -q` risponde senza accenderla, e il primo comando che ha bisogno del motore la fa ripartire, con ogni container che ha una politica di riavvio | i container degli altri progetti si credevano fermi finché qualcuno non li riaccendeva (#73); nessuno aveva previsto che ripartissero da soli | c'era già, e ha retto: gli script di misura fermano all'avvio ogni container acceso, e prima di **ogni run** la guardia pretende `docker ps -q` vuoto (`MEASURE_AB_GUARD`), quindi una VM che riparte a metà ferma il confronto alla run dopo invece di finirci dentro. Nuovo: `orphans.sh` elenca i container con lo stesso `docker ps` semplice della guardia e filtra da sé quelli dell'immagine `trochilus-dev` (i nostri, rimasti da un `make check` interrotto), senza il filtro che chiede al motore; i container degli altri progetti restano compito della guardia, non di `orphans.sh` | aperta: quando la guardia ferma una sessione per i container deve dire quali e da quanto è accesa la VM; dentro una misura nessun comando Docker oltre `docker ps -q` (oggi è così: il `docker run` che svuota la cache gira prima di fermare i container) | io, vedendo sei container accesi dopo averne fermati otto |
| 88 | 2026-09-19 | errore | **la mediana nascondeva uno stimatore che tira a sorte.** La larghezza del decode la sceglie ogni sessione (pool, metà, un quarto; tre passate per larghezza; la più ampia entro l'1% dalla più veloce). Contando le scelte run per run, invece di guardare la velocità mediana: a contesto 512 coi `yes` sceglieva 4, 8 e 16 con 4 cambi su 8 run, a 2048 3-5 cambi; a macchina rumorosa (indicizzatore di Windows) 4×4 1×8 3×16; a macchina quieta sbaglia ancora una run su otto. Il margine dell'1% è sotto il rumore di tre passate da un token (2-5%), ed era stato stretto da 3% a 1% **sulla macchina coi quattro core presi**, dove 16 thread perdevano davvero (#71, #84) | si guardava la velocità mediana di `auto`, che a 512 è buona con ogni larghezza (4, 6, 8 e 16 stanno entro il rumore): una scelta instabile che non costa velocità non si vede in una mediana | `tools/decode_context_report.py speed` stampa per ogni modo e contesto quante volte è stata scelta ogni larghezza e **quanti cambi** da una run all'altra; le sessioni `widths` di `threads_phase.sh` e `decode_context.sh` danno la verità forzata a ogni contesto. Lo stimatore va **riscritto**, non ritoccato (domanda 31, punto 0 dei prossimi passi): margine dal rumore misurato nelle passate e non da una costante, più giri se non decide, isteresi nelle rimisure, a parità la larghezza **stretta** (su macchina pulita non perde mai, sotto il carico di altri vale il 10%); test deterministici su tempi sintetici, validazione a macchina quieta. **Riscritto la sera stessa**: `tr_decode_tune_stats`/`pick`/`debounce` e la sessione provati in `tests/test_phase.c` con un orologio finto (rosso prima: il caso piatto con l'attesa «la stretta» dava `0 != 2`), dieci mutazioni viste rosse (`tools/mutate_tune.sh`), la storia delle scelte nella riga `threads:` e `decode_context.sh long` per contare i cambi sul modello vero | aperta: la validazione sul modello vero, di notte (domanda 31, punto 0) | Marcello, leggendo i cambi di larghezza; i numeri dal report nuovo |
| 89 | 2026-09-19 | errore | `tools/orphans.sh`, **al primo uso vero, segnalava come orfano lo script che lo chiamava** e ha fermato la catena di misure prima della prima run: per escludere i chiamanti risaliva la catena dei padri di Windows, che su MSYS si spezza a ogni `exec` (il processo creatore muore e il nuovo programma resta con un padre che non esiste più) | scritto e mai eseguito dentro uno script di misura: provato a mano da una shell, dove la catena è corta e regge | la catena dei chiamanti la calcola MSYS (`/proc/<pid>/ppid` e `winpid`) e passa allo script PowerShell; visto verde quando chi chiama ha sulla riga di comando il nome di uno script di misura, rosso con un `yes` staccato, verde dopo; poi dal vivo sotto `measure_begin` in cinque script di fila. È il motivo per cui in questo registro una prevenzione scritta e non ancora vista rossa e verde si dichiara «da provare» | chiusa | il primo uso vero, dieci secondi dopo il lancio |
| 90 | 2026-09-19 | errore | `tools/platform_bits.sh` è **uscito con 0 dopo che un suo passo era fallito** (lo strumento `dump_rope.exe` bloccato da Smart App Control, «Permission denied»): il blocco dei confronti finiva in una pipe verso `tee`, e l'uscita di una pipe è quella di `tee`; il rapporto si fermava a metà e sembrava finito | sotto `set -e` un errore a sinistra di una pipe non ferma niente (la shell di sistema non ha `pipefail`) | ciò che può fallire scrive su file e il file si mostra con `cat`: `platform_bits.sh` gira il corpo in una subshell con `set -e` e ne porta fuori l'uscita, e il confronto delle tabelle RoPE sta in coda e si salta con un messaggio se lo strumento non parte ancora; corretti allo stesso modo `expf_quality.sh` e `remeasure.sh`; `tools/lint.py` rifiuta in `tools/*.sh` ogni pipe verso `tee` che non parta da un `echo` o da un `printf`, con campioni (i due casi di prima sono fra i campioni rossi) | chiusa | io, leggendo «exit=0» sotto un «Permission denied» |
| 91 | 2026-09-19 | scoperta | **un agente è morto a metà lavoro col riavvio del PC**, e la ripresa è costata poco: il passo 0 di STATO diceva già dove si sarebbe visto il lavoro a metà (`git diff src/models tests/test_phase.c`). Trovato: le tre funzioni e la sessione scritte e coerenti col progetto, i test fermi alla vecchia interfaccia (non compilavano). Si è tenuto il codice e rilanciato un agente solo sul resto | un agente non lascia traccia di dove è arrivato: la lascia solo l'albero di lavoro, e bisogna sapere dove guardare | *regola*: quando un passo va a un agente, STATO dice prima quali file mostrano il lavoro a metà e qual è il criterio per tenerlo o buttarlo; il brief di ripresa parte dalla rilettura del diff contro il progetto, non da un `git checkout` | aperta (regola) | io, riprendendo il passo 0 |
| 92 | 2026-09-19 | scoperta | **la sessione che misura la larghezza non si poteva provare ramo per ramo**: l'estensione sulla coppia, la rimisura alla classe di contesto, il cambio pendente dopo 128 passate dipendono dai tempi veri delle passate, e un test sull'orologio vero prende un ramo o l'altro a caso (è lo stesso difetto dello stimatore, #88, portato nel test). Le tre funzioni pure non bastavano: le mutazioni della sessione sarebbero rimaste verdi | il cronometro della sonda era `tr_time_sec()` chiamato dentro `session_eval`, senza un punto dove il test potesse mettere i suoi tempi | `tr_session_set_tune_clock` (solo in `model_internal.h`, per i test): un orologio finto che dà a ogni sonda il tempo scritto in tabella per la larghezza attiva; `tests/test_phase.c` verifica i thread di ogni passata con un contatore per ramo, e `tools/mutate_tune.sh` ha visto rosse le quattro mutazioni della sessione e le due della storia | chiusa | io, scrivendo il brief: prima dei test, non dopo |
| 93 | 2026-09-20 | scoperta | **due pezzi del piano di M1 scritti a tavolino non reggono alla prima misura.** Il piano diceva «pin dall'uso» e «pool di I/O». Sulla traccia del routing (codice, 904 + 300 token) il pin statico degli esperti più usati nel prompt perde contro una LRU a ogni capacità (al 75% legge il triplo: 94 contro 32 MiB per token); sul disco di questa macchina un lettore solo prende quanto otto (~1.5 GB/s) e sedici perdono. E un terzo pezzo, scritto da me il giorno stesso: «precaricare i 12 previsti» (la soglia della domanda 13) con questo disco **toglie** il 40% dei token al secondo nel modello a tempo, perché ogni candidato sbagliato è una lettura e la lettura è il collo: la soglia misurava gli esperti indovinati, non i byte letti | il piano era preso dalle fonti (colibri tiene un pin dall'uso, tutti hanno un pool di I/O) senza un numero nostro davanti | le misure 13-16 stanno **prima** del progetto di M1 e restano riproducibili: `trochilus run --route-trace` + `tools/route_trace_report.py` (con `--check` in `make lint`), `make bench-disk`. Il progetto di M1 parte da quei numeri: LRU, 1-2 thread di I/O, precaricamento un layer prima | chiusa per M1; *regola* per i prossimi piani: un pezzo preso da una fonte entra nel piano con la misura che lo giustifica, o con la domanda in MISURE | io, leggendo il report |
| 94 | 2026-09-20 | scoperta | `tr_file_pread` su Windows **crea e chiude un evento a ogni chiamata** (`CreateEventW`), e rilegge a pezzi da 256 MiB: oggi legge ogni tensore una volta al caricamento e non si vede, ma in M1 diventa il percorso di ogni esperto mancato, migliaia di volte al secondo | scritto per il caricamento (M0), dove una chiamata in più per tensore non conta | **misurato prima di cambiare, e non valeva**: `make bench-disk` in nativo confronta la chiamata di oggi con la stessa lettura su un evento tenuto vivo (`bench_event_overhead`, 20 000 giri su un blocco già in cache): 2.54-2.61 µs contro 2.11-2.20, cioè **0.41-0.44 µs a chiamata su tre run**, lo 0.03% di una lettura da 2 MiB (1.4 ms su questo disco). Il codice resta com'è; la misura resta nel banco, così su una macchina con un disco molto più veloce la si rifà invece di riaprire la discussione | chiusa: costo reale sotto la soglia | io, scrivendo il banco del disco; misurata il 2026-09-20 |
| 95 | 2026-09-20 | errore | **un agente ha riscritto quattro file sorgente con i fine riga di Windows** (`model.c`, `model.h`, `model_internal.h`, `tests/test_experts.c`: CRLF su ogni riga; in HEAD sono LF e `core.autocrlf` è spento). Compilava e passava `make check`; in git sarebbe stato il diff dell'intero file, e ogni sostituzione di testo su più righe scritta con LF non trovava più niente (così l'ho visto: una sostituzione che doveva trovare una riga ne trovava zero) | lo strumento di scrittura dell'agente su Windows ha salvato con CRLF; niente nel cancello guardava i fine riga fuori da `docs/` | `tools/lint.py` `check_line_endings`: nessun ritorno a capo in `src/`, `tests/`, `tools/`, `bench/` (tranne `bench/results`), `.claude/` e nel Makefile; visto rosso con un file di prova | chiusa | io, rileggendo il lavoro del lotto 2 |
| 96 | 2026-09-20 | scoperta | **confrontare il motore con se stesso sotto budget diversi non vede un errore fatto al caricamento.** Nel lotto 2 di M1 la mutazione «posizioni di gate e up scambiate» restava verde: lo scambio è uguale con ogni budget, quindi i logit sbagliati erano identici fra loro. Stessa famiglia di #43 e #78: risultati uguali non dicono che siano giusti. Nello stesso lotto un archivio «residente» in realtà si riempiva a richiesta, e i logit erano giusti lo stesso | il test principale di M1 è un'uguaglianza fra due esecuzioni dello stesso codice; serve anche un riferimento che non passi da quel codice | `tests/test_stream.c` gruppo `content`: i byte che l'archivio consegna confrontati con `tr_gguf_read_range` sul file; gruppo `resident`: dopo il caricamento i mancati sono tutte le unità e poi non crescono più; gli oracoli contro transformers girano anche sotto `TR_EXPERT_BUDGET_MIB=min`; mutazione rossa in `tools/mutate_stream.sh` | chiusa | l'agente del lotto 2, con la mutazione che non diventava rossa |
| 97 | 2026-09-20 | scoperta | **gli script delle mutazioni copiavano l'ambiente Python del progetto a ogni mutazione**: `cp -r Makefile src tests tools bench /tmp/mut/` porta con sé `tools/.venv`, 706 MiB, attraverso il bind mount di Windows. Una passata di `mutate_experts.sh` è arrivata a 28 minuti per 10 mutazioni su 16; senza `.venv` la stessa passata ne fa 16 in 56 secondi, **trenta volte più veloce**. Stessa riga in `mutate_kv`, `prefill`, `route`, `stream`, `tune`: le mutazioni della traccia costavano 25 minuti invece di uno | il copione copiava `tools/` per intero perché qualche script serve nella copia; il container però ha il suo Python (`PY=/opt/venv/bin/python`), quindi `.venv` non serviva mai | la copia salta `.venv` e `__pycache__` in tutti e sei gli script (`find tools -maxdepth 1 ... ! -name .venv`); provata con la sola riga di controllo, che ora è verde in pochi secondi | chiusa | l'agente del lotto 3, a cui una passata di mutazioni non finiva |
| 98 | 2026-09-20 | scoperta | **una domanda è stata chiusa su una simulazione, e per un giorno il progetto di M1 ha poggiato su quel numero.** La domanda 14 diceva 22.4 unità mancate e 143 MiB per token generato con metà modello in RAM, e da lì venivano gli «attesi 7.5 tok/s». Misurato sul motore vero: **0.3 unità e 1.8 MiB** subito dopo il prompt, **0.03 e 0.2** lontano dal prompt, e 24-32 tok/s. Nessun conto sbagliato: la simulazione girava su una generazione lunga partendo da una cache qualunque, il motore entra nel decode con la LRU appena riempita dal prompt, che legge comunque tutto il modello | un numero calcolato ha la stessa forma di un numero misurato, e la riga della domanda era già barrata come chiusa: da lì in poi nessuno l'ha più riletta come un'ipotesi | **controllo**: `tools/check_measurements.py` in `make check` — una riga di `docs/MEASUREMENTS.md` che dà un numero da simulazione o da modello a tempo porta il tag «modello, non misura» (o «modello superato dalla misura» se è storia), e una domanda tagliata col primo tag **non può essere barrata come chiusa** | chiusa | la misura `experts_budget.sh misses`, dopo il codice: nessun test poteva trovarlo, era una lettura sbagliata di un numero giusto |
| 99 | 2026-09-21 | scoperta | **il prefill sotto budget rilegge la tabella degli esperti a ogni passata.** Il prompt si elabora a blocchi di 512 token (`OLMOE_DEFAULT_BATCH`) e ogni passata percorre tutti i layer: a 2048 token l'archivio legge **22 880 MiB invece di 6 528**, 23.51 s invece di 11.27. Per un giorno abbiamo scritto «il prompt legge tutto il modello una volta» in STATO, perché l'unica lunghezza misurata era 512, dove le passate sono una sola | nessuno aveva guardato i byte letti accanto alla lunghezza del prompt: il numero c'era in ogni riga `experts:`, ma le misure di M1 giravano tutte a un prompt solo | **controllo**: `tests/test_stream.c` §`once_per_prompt` (in `make check`) — un prompt di 36 token in passate da 12 sull'archivio minimo deve leggere al massimo `n_units + n_slots` unità; visto rosso prima del fix (33 su 16 di tabella), verde dopo. La cura è l'**ordine per layer** (per ogni layer tutte le passate), non `-b` grande, che sposterebbe il problema sulla memoria delle attivazioni. *Regola* che resta: ogni misura nuova gira ad almeno due lunghezze di prompt | chiusa | la misura `prefill_overlap.sh`, scritta per un'altra domanda (la sovrapposizione I/O-calcolo) |
| 100 | 2026-09-22 | error | **an agent's report is not evidence.** A Haiku asked to translate `MEASUREMENTS.md` reported "translation complete, all sections preserved": it had translated the headings and the first third of the bodies, leaving **1500 lines in Italian**. A second agent on three other files delivered properly — nothing in the two reports told them apart | a report is generated text, not a measurement of the file; and an intact structure (numbers, tables, headings) says nothing about the prose, which is exactly what a translation is supposed to change | **check**: `tools/check_translation.py <file>` compares the file with its committed version and prints numbers lost or added, table rows, fenced blocks, heading count and **how many Italian words are left**; `tools/italian_map.py <file>` says which sections they are. A translated file is not committed before it passes. *Rule*: for delegated work, verify the product, never the account of it | closed | the automatic check, not the report |
| 101 | 2026-09-22 | error | **`pathlib.write_text` writes CRLF on Windows.** The sweep that rewrote the references to the renamed files turned 97 files from LF to CRLF in one go, because `write_text` opens in text mode and translates every `\n` | the script read with `read_text` (which normalises to `\n`) and wrote back with `write_text` (which translates again): a round trip that is not symmetric, and invisible on screen in a diff | **check**: it already existed — `tools/lint.py` rejects a file with a carriage return (LESSONS #95) and caught all 97. *Rule*: a script that rewrites files of this repository uses `read_bytes`/`write_bytes`, never the text variants | closed | `tools/lint.py` |
| 102 | 2026-09-22 | error | **the gate was red on `main` from the translation commit on**: `tools/check_measurements.py` looked for the Italian tag, line by line, and the English `MEASUREMENTS.md` said "model beaten by" at the end of one line and "measure" on the next. Two commits went in on a red gate. The same line-by-line reading had a second hole: rule B (a question closed on a model's number) only saw a tag on the row's first line | a checker that reads lines checks the layout, not the text: the wrapping of a sentence decided the verdict | **check**: the checker reads logical blocks (a table row with its wrapped lines, a paragraph), knows the English tags "model, not measured" / "model superseded by measurement" beside the Italian ones, and runs 6 self-test cases first, every time, 4 of them red under the old reading. *Rule* (already there, broken): nothing is committed without `make check` green | closed | me, first `make check` of the review |
| 103 | 2026-09-22 | error | **M1 on Windows could not read an expert that ends the file.** On a direct handle NTFS returns the last, partial sector short (777 bytes), `tr_file_pread` asked for the rest from the unaligned offset where it stopped, NTFS refused it (error 87) and the read failed. `test_file_direct` covers exactly this case, and was green, because the gate runs it in Linux, where ext4 answers 0 at the end of the file. Seen red natively on the binary of 2026-09-21 | the gate runs the C tests only in the container (Smart App Control, #12): the Windows branches of `src/base` never ran in it; and every link wrote a new timestamp in the PE header, so every rebuild was a new hash for Smart App Control to block | **check**: a direct read that stops short of a sector boundary has met the end and returns (both platforms); `make check` on Windows runs every C test natively too (`tools/native_tests.sh`, a blocked binary is SKIPPED and says so); links use `-Wl,--no-insert-timestamp` (same code, same bytes, same verdict) and `test_base` links `src/base` only, so its hash changes only with it | closed | me, review of `src/memory` and `src/base` (Opus 5.5) |
| 104 | 2026-09-22 | error | **the pool's slots were not one per cache line, as the code said**: `slot` is 72 bytes and the `union { slot; char[64]; }` packed them 72 bytes apart, so worker k's `state` shared a line with worker k-1's `begin`/`end`, which the dispatcher writes on every call (k mod 8 in 5..7). Not measured: one line bounce per call | a padding union is only as big as its largest member: `char[64]` pads nothing once the struct outgrows it | **check**: `_Alignas(64)` on the slot and a `_Static_assert` that a `slot_line` fills whole lines, which fails to compile on the old layout | closed | me, review of `src/base/threads.c` |
| 105 | 2026-09-22 | error | **the GGUF arena said 16-byte alignment and gave 8**: its data started right after a 24-byte block header, so every key, string and metadata array sat at 8 mod 16. Harmless for today's scalar readers, a trap for the first SIMD one | alignment of the first byte after a header is the header's size modulo the alignment, not the allocator's | **check**: the header is rounded up to 16; `test_gguf` checks every key and array pointer modulo 16 (red: 8 != 0) | closed | me, review of `src/format/gguf.c` |
| 106 | 2026-09-22 | error | **generating a token allocated on Windows** (4 allocations at 8 threads, `test_hot` red natively, green in Linux): MinGW compiles `_Thread_local` as emulated TLS, which mallocs a variable's storage on a thread's first touch, and a worker first touches `t_depth` in its first chunk of work, which for half the workers came after the first token | Linux has native TLS; the hot-path contract was only ever checked there | **check**: `touch_tls()` when a thread joins a pool (worker start, and the caller in `tr_pool_create`); `test_hot` runs natively in `make check` (#103). Linux cannot show it (clang `-femulated-tls` takes emutls from the shared `libgcc_s`, which `--wrap` does not see) | closed: `test_hot` green natively in `make check` (18 native tests passed) | me, first native run of the C tests |
| 107 | 2026-09-22 | error | **`tools/test_cleanup.sh` said "a few seconds" and took 101 s of every `make check`**: the victim without the trap leaves its `ping -n 98` behind, as it must, and that orphan had inherited the stdout of `$(stop_victim ...)`: the command substitution waited for its whole ping. Found also by running the step by hand while the gate was running it: the two runs share the ping counts, and each saw the other's child | a command substitution returns when every writer of its pipe has closed it, not when its own command ends; an orphan kept on purpose is a writer | **check**: the victim writes to `/dev/null`: 5 s, both halves still seen. *Rule*: a step of the gate is not run by hand while the gate runs | closed | me, per-step times of the gate |
| 108 | 2026-09-22 | error | **the profiler's clock was calibrated on one unguarded pair of reads**: `tick_init` read the OS clock, then RDTSC, waited 20 ms, and read them again; a preemption between two of those reads went straight into the rate, 1 ms of it being 5% on every time the profiler printed in that process. Found because `test_prof` (ticks against the OS clock over 100 ms, within 3%) failed under ASan on a loaded machine: the test had the same single-pair weakness | a rate from two instants is only as good as each instant, and "an instant" read with two instructions is two instants when the thread can lose the CPU between them | **check**: `tr_prof_calibrate` reads each end bracketed (ticks, clock, ticks; narrowest of 8); `test_prof` calibrates on fake clocks with a 2 ms stall moved over each of the 80 reads at the two ends of the window: worst error 0.00000%, and 9.9995% (red) on the old calibration; the real-clock check takes the best of 3 windows | closed | the gate, flaky `test_prof` |
| 109 | 2026-09-22 | discovery | **one step was 42% of `make check`**: `oracle-real` under the smallest expert store took 306 s of the gate's 797, because the real model cut to 2 layers (`fixtures/olmoe-2layer`, 1 GB) was read over the Windows bind mount, one expert miss after another, while the real model itself had moved to the ext4 volume long before (#41). On the volume: 57 s | #41 moved the file that was slow, not the files derived from it; and the gate printed no times, so 5 minutes hid in 14 | **check**: the cut lives beside the real model (`REALFIX := models/olmoe-2layer`, the volume in the container); the gate's steps now run with their times visible (`build/check-*.log`, per-step table in `docs/MEASUREMENTS.md` §The gate). *Rule*: a file the container reads heavily lives on the volume | closed | me, per-step times of the gate |
| 110 | 2026-09-22 | discovery | **green tests that could not see their own lines**: the first generated mutations (`tools/mutate_auto.py`) left alive 89 of 221 mutants of `gguf.c`, 27 of 100 of `experts.c`, 33 of 75 of `threads.c`. Among them: `tr_gguf_get_f32` and `tr_gguf_get_bool` called by no test; the reader's path for values over 1 MiB never taken; no FLOAT64 value, no 4-dim tensor, no alignment 0; the range check of `tr_gguf_read_range` "tested" on the last tensor of the file, where the read fails at the end of the file anyway, so the check itself could be deleted; `tr_experts_part` never asked out of bounds; `tr_parallel_for` never given an empty range | the curated `tools/mutate_*.sh` try the mutations their author thought of, in the areas they were written for; a test is seen red once, for the branch it was written for, and nothing asks what the rest of the file's lines are guarded by | **check**: `tools/mutate_auto.py` generates the mutations for any file (operators, off-by-one, return codes, negations), sets aside the ones whose object comes out identical, and lists survivors; `test_gguf`, `test_experts`, `test_base` gained the cases above (survivors in `docs/MEASUREMENTS.md` §Generated mutations). *Rule*: a file touched in a review goes through `mutate_auto.py`, and its survivors are either killed or named as equivalent | open: every file of the review went through it (2026-09-23, `tools/mutate_files.sh`); the survivors of `tokenizer.c` (75) and `main.c` (334) are not yet read one by one | me, `tools/mutate_auto.py` |
| 111 | 2026-09-23 | error | **the command line sized its buffers from the numbers it was given**: `generate -n`, `-p`, `run -n` and `chat -n` allocated `n * sizeof(int32_t)` bytes (plus the prompt), which for n near 2^62 wraps to a few bytes; the tokens were then written past them (exit 134 or 139 without ASan, silent corruption at best). And `--tokens` cast a `long` to `int32_t`: `4294967297` became token 1 on Linux, and was evaluated. Also `--profile-json` wrote a control character of the model's path raw, which is not JSON | `src/app/main.c` had no test of its own: the oracles and `check-tiny` run it on the happy path only, so nothing ever handed it a number out of range; a buffer that grows with a user number was sized from the number, not from what bounds it | **check**: `tests/test_cli.c` runs the binary built beside it (Linux, ASan and native Windows) on a synthetic model that carries a tokenizer and OLMoE's chat template (`synth_with_tokenizer`); red before the fix on all seven cases (exit 0, 1, 134, 139, a UBSan store), green after: the buffers are sized from the context (`tr_greedy` never writes past it), an id outside 0..INT32_MAX is a usage error, a control character is `\u00XX`. *Rule*: a buffer that grows with a number from outside is sized from what bounds it | closed | me, review of `src/app/main.c` |
| 112 | 2026-09-23 | discovery | **three branches of `olmoe.c` had never met transformers, and its whole load-time validation had never met a bad file**: `norm_topk_prob` (the chosen experts' weights summed to 1), `clip_qkv` and a `rope_theta` read from the file: the tiny fixture leaves all three at their defaults, and the synthetic C tests prove that every split of the work gives the same bits, never that the bits are right. `mutate_auto.py` over the C tests and the tiny oracle left 92 of 400 mutants of `olmoe.c` alive: those three, a missing key, each zero hyperparameter, wrong tensor shapes, a missing tensor, an unsupported type, `eval` of 0 tokens or past the context, the router's tie rule, the profiler's byte counts | a model file's C tests are invariance tests: a mutation that changes every path the same way passes all of them; and a fixture made with the defaults never runs the branches the defaults switch off | **check**: `fixtures/tiny-olmoe-opts` (`make_tiny_olmoe.py --options`: `norm_topk_prob`, `clip_qkv` 1.0, `rope_theta` 500000) in `make oracle` (16/16 tokens, logits within 2.1e-7); `tests/test_model_load.c` (malformed files through the new `synth_olmoe.h` knobs, eval bounds, mask, ties to the lower id, the trace of a full batch); `test_model_prof` counts exact expert and KV bytes; `mutate_auto.py --cmd` runs the oracles on every mutant, and `tools/mutate_files.sh` knows which checks guard which file. *Rule*: a fixture for every option the model family has, not only the one the first model uses | closed | me, `tools/mutate_auto.py` on `olmoe.c` |
| 113 | 2026-09-23 | error | **the synthetic tokenizer declared 10 keys and wrote 9**: the reader took the first tensor for a tenth key ("string length 73183493944770560 out of range"). Caught at once by the test that used it | a count kept by hand beside the code it counts | **check**: the count is summed as the keys are written (`n += synth_kv_u32(...)`), and `test_cli`/`test_model_load` load every such file | closed | the test, first run |
| 114 | 2026-09-23 | error | **a check copied the whole repository into the container** (`cp -r /src`), `models/` included (7 GB over the bind mount): ten minutes before it was stopped | a copy meant for the sources, written as a copy of everything | *rule*: a copy into the container takes `src tests Makefile`, as `mutate_auto.py` does; the models are never copied, they are read where they are (#109) | open: rule | me |
| 115 | 2026-09-23 | discovery | **a mutation run under memory pressure killed mutants no test could see**: a second run of `olmoe.c` with the same tests left alive 21 mutants the first had killed, among them the `n_logits` checks of `olmoe_eval`, which no test calls. During the first run the Docker VM (15 GB) was full: twelve tokenizer oracles of 3-4 GB each (its full Unicode sweep), which the VM's OOM killer stopped 12 times, once on behalf of another window's MySQL; model tests that load a 600 MB model under a memory guard reading the real free RAM; my own runs beside them. A load refused is a test failed, and a test failed was a mutant killed | `mutate_auto.py` took one failure for a kill; its jobs were sized to the processors (32), not to the memory; and the load of other windows counted against the same VM unseen | **check**: `mutate_auto.py` runs a failed check again on the same mutant, and only a second failure kills it; a failure that does not repeat is printed `FLAKY` (the recheck of `olmoe.c`: 2 of `test_prefill`, gone on the retry); `tools/mutate_files.sh` sizes its jobs to the memory (6 for a model file, the tokenizer oracle without its sweep: 394 MB and 6 s instead of 4 GB and 31 s, the sweep in a run of its own at 2 jobs). *Rule*: before a campaign, `docker ps` and `free -m` in the VM; nothing else loads it meanwhile | closed | me, two runs of the same file compared |
| 116 | 2026-09-23 | error | **a mutant applied by hand landed on the real source**: to see a new test red, a `sed -i` on `src/models/olmoe.c` ran in a container whose working directory is the bind mount of the repository, not a copy; `git diff` showed it at once and `git checkout` took it back, before any copy of the tree was made from it | the same command line works in a copy and in the checkout, and nothing tells them apart | *rule*: a mutant by hand goes into a copy (`cp -r src tests Makefile /tmp/r`, #114), as `mutate_auto.py` does; `git status --short src/` after it | open: rule | me, `git diff` right after |
| 117 | 2026-09-23 | discovery | **a timed-out mutant was neither listed nor run again**: a second run of `tokenizer.c` showed three survivors the first had not (an out-of-memory `return`, the 1 GiB text limit, byte 0 as a one-byte piece). None of them can loop: under 12 jobs with ASan their checks had run out of time, and "timed out" was read as caught | a timeout taken for a detection: the shape of #115, on the other verdict | **check**: `mutate_auto.py` runs a check that ran out of time again with three times the budget, and lists a mutant that times out twice as `TIMEOUT`, read like a survivor | closed | me, comparing two runs |
| 118 | 2026-09-23 | discovery | **the added tokens were counted and filled by two copies of one predicate**, and the text buffer is sized from the token strings (`strlen`), larger than the decoded bytes: a mutant of the count only resized an allocation, and one that took an empty control token read its "first byte" past the decoded text but inside the allocation, where ASan does not look | two copies of a condition that must agree; a buffer sized for more than it holds | **check**: one helper (`is_added`) for both loops; `test_tokenizer` has an empty control token and encodes a text of every byte value, so a mutant that matches it loops (timeout) | closed | `mutate_auto.py`, second run |
| 119 | 2026-09-23 | discovery | **a memory refusal that lasted killed mutants twice**: `tokenizer.c:280` (`malloc(total >= 0 ? ...)`, equivalent) and an out-of-memory check came out killed in a 12-job run and survived alone with the same three checks. `test_cli` loads a model under the engine's memory guard, and in the VM (12 ASan jobs, the other windows' containers, and a mutant whose emit loop eats memory) the guard refused on the first run and on its repeat: #115's "fail twice" does not help when the pressure outlasts both | a refusal of the machine read as a verdict on the mutant | **check**: `mutate_auto.py` reads a failed check's output; the guard's "not enough memory" or a SIGKILL is no verdict, that mutant is judged again at the end, alone, and listed as `PRESSURE` if refused again. The counts of the other files predate this and #117: they may hide survivors until they are run again | closed (tool); the other files' counts open | me, a mutant I had read as equivalent came out killed |
| 120 | 2026-09-23 | debt | **the command line parsed its numbers with `atoi`/`atoll`, in five copies of one loop**: `-n abc` was 0, `-n 5x` 5, `-t -3` the default; an option given last had no value to take and the loop fell into the usage without naming it. And 334 of 646 mutants of `src/app/main.c` lived: `inspect`, `cpu`, every option error, `tokenize --pieces/--decode/-f/--batch`, `--expert-mask`, `--route-trace`, `--profile`, the threads line, the chat's system prompt and `/reset`, `chat-template`'s records had never run under a test, and the chat's UTF-8 cut was a static function no test could reach | each command grew its own loop, and `test_cli` tested only the paths that had already broken (#111) | **check**: one table of options per command and one parser (`parse_opts`; `parse_num`: decimal digits only, inside the option's own range, else exit 2 naming the option); one reader of `--batch` records for `tokenize` and `chat-template`; the UTF-8 cut is `tr_utf8_whole_prefix`, tested in `test_unicode`. `tests/test_cli.c` runs every command, every usage error alone, every range at both ends, every kind of option without its value, and checks stdout and stderr apart. Survivors in `docs/MEASUREMENTS.md` §Generated mutations | closed | me, survivors of `main.c` read one by one |
| 121 | 2026-09-23 | error | **`--expert-budget` wrapped at 2^44 MiB**: `(uint64_t)mib * 1024 * 1024` is 0 there, and a budget of 0 means automatic, so a forced number became no number at all | a product of a number from outside, with no bound | **check**: `parse_budget` takes 1 to 2^43 - 1 MiB (what 63 bits of bytes hold) or `min`; `test_cli` accepts the top and refuses one past it | closed | me, reading `parse_expert_budget` |
| 122 | 2026-09-23 | error | **a mutant that timed out left its looping process running for hours**: `mutate_auto.py` killed a check out of time, but `test_cli` runs the binary through `system()`, and the `trochilus` it started (a `--batch` record that no longer advances, an `fscanf` that no longer consumes) kept looping: seven of them, 100 minutes old and eating the CPU of every file that came after `main.c`, found only because `olmoe.c` took an hour | `subprocess.run(timeout=)` kills the child it started, not the children that child started; and nothing counted what a run left behind | **check**: every check runs in a session of its own and a timeout kills the whole session (`os.killpg`); at the end the run counts the processes still working in its trees, kills them and says how many in its last line, exiting 1 if any. Red before the fix (a check whose grandchild loops: 1 left behind), green after (0) | closed | me, `docker exec ps` while a campaign ran slow |
| 123 | 2026-09-23 | discovery | **a mutant that breaks the engine's memory plan reads like a machine short of memory**: six mutants of `olmoe.c` (the context length's default, a part's size, the forced-budget branch, the plan's verdict) made the engine refuse its model with "not enough memory", the words #119 takes for the machine's; each was judged again alone at the end, refused again, and listed as `PRESSURE`, to be read by hand, at a cost of an hour of serial runs (the per-check budgets of #124 make it minutes) | a message that is both the machine's and the mutant's | *rule*: a `PRESSURE` line is read by hand, with the check that refused named in the progress log (#125). The first prevention, a refused mutant counted as killed when the unmutated tree had passed alone, was taken back: it went with kills that did not repeat (#125) | open: rule | me, reading the `PRESSURE` lines of `olmoe.c` |
| 124 | 2026-09-23 | discovery | **a mutation run cost hours where minutes would do**: the budget of every check was ten times all the checks together (for `olmoe.c` ten model tests and three oracles, about 20 minutes), so one mutant that looped cost 80 minutes of a job with its 3x repeat; a killed mutant ran its whole test to the end; the test pools of twelve jobs pinned their threads to the same cores and spun on them; and every run mutated the whole file, whatever had changed | a budget sized once for the worst case, and no way to ask how far a run was | **check**: each check gets ten times its own time, fastest first; `TR_TEST_FAILFAST=1` stops a C test at its first failure (`tests/test.h`); `tools/mutate_files.sh` runs the tests with no pinning and no spinning, writes progress with the time left to `build/mutate/<name>.progress`, and with `CHANGED=<ref>` mutates only the lines that differ from that ref (`main.c` after today's change: 114 mutants instead of 418) | closed | Marcello, "we can't wait all this time every time" |
| 125 | 2026-09-23 | error | **five kills of a clean `olmoe.c` run did not repeat**: the run reported killed five mutants the earlier runs had left alive (a read error's `return -1`, an allocation check, the direct-read probe, `TR_MEM_AVAILABLE_MIB > 0`, and `reserve < two_gib` made `<=`, which gives the same value); judged again in a run of their six lines, all five passed every check, and one of them, run by hand alone through all thirteen checks, passed too. Not known which of two ways killed them: the refused-then-killed rule of #123, or a check failing twice under the load of six model-test jobs (#115 again) | the report kept only the verdicts, not which check gave each, so a kill could not be audited | **check**: every progress line names the verdict and the check that gave it with its exit statuses (`... \| src/models/olmoe.c:495: < to <=: killed by ./b/tests/test_x (1, 1)`); the #123 rule is taken back; the five are survivors again in `docs/MEASUREMENTS.md` | closed (the cause shows in the next run's log) | me, a mutant I knew to be equivalent reported killed |
| 126 | 2026-09-23 | error | **the new `test_cli` failed natively on Windows, 19 checks**: it looked for lines ending in "\n", and on Windows `cpu`, `inspect` and every message on stderr end in "\r\n" (text mode); only `run`, `chat` and `tokenize` write bytes. Written and seen green in the Linux container only, because Smart App Control had blocked the native binary all day | a comparison of printed text that assumed one platform's line ending | **check**: `test_cli` turns "\r\n" into "\n" in everything it captures, on both sides of every comparison; the native run in `make check` (`tools/native_tests.sh`) is what caught it | closed | `make check`, native tests |
| 127 | 2026-09-23 | error | **the wait for the machine's marker ran before the measuring scripts had their traps**: `measure_begin` gained a wait of up to 6 hours for another window's `~/.claude/macchina-ferma`, after taking the lock and starting `tools/stay_awake.ps1`, while the six scripts set `trap measure_end EXIT` only after `measure_begin` returned: an interrupted wait left the lock and kept the machine awake for 4 hours. And two races: a missing marker was removed anyway (another window's, created in between, would go), and any `trochilus:` line was taken for our own leftover (a live measurement of another checkout would lose it) | a long wait added inside a function whose callers assumed it returned at once | **check**: the traps come before `measure_begin` in all six scripts, and the lock, the marker, `tools/orphans.sh` and `stay_awake` go in that order; a marker is removed only if it exists and is stale or ours with a dead pid; `tools/test_marker.sh` (10 branches, in `make check`) and `tools/mutate_marker.sh` (9 mutations, all red) | closed | review agent (Opus 5.5) |
| 128 | 2026-09-23 | error | **a mutation harness left `tools/measure_guard.lib` mutated in the working tree for 25 minutes**: a Python script in the scratchpad wrote each mutant over the real file and ran the test with `subprocess.run(timeout=...)`; on the mutant that removes the wait's cap the test never ends, and on Windows `run` killed `sh` but then waited forever in `communicate()` for the pipe its grandchildren still held. Found by comparing the file with its backup; an orphan `test_marker.sh` of an earlier run had been looping for 30 minutes too | a mutant written over the tracked file (#116 again), and a timeout that kills the child but not what it started (#122 again, outside `mutate_auto.py`) | **check**: `tools/mutate_marker.sh` mutates a copy the test loads over the real library (`MEASURE_GUARD_LIB`), caps each run with `timeout` (its whole process group), and counts and kills what a run left behind. *Rule*: no mutation harness writes over a tracked file; the test takes the mutated copy | closed | me, `cmp` with the backup |
| 129 | 2026-09-23 | error | **the Windows side of `trochilus serve` had two bugs the Linux run could not see**: `_open_osfhandle` without `_O_TEXT` opens a stream in binary, so `generate` (which prints text) lost its "\r" through the server; and a second server on the same pipe name gets `ERROR_PIPE_BUSY`, not the documented `ERROR_ACCESS_DENIED`, and said "cannot listen" instead of "already running" | a Windows branch written against the documentation and the CRT's supposed defaults, checked first in the Linux container | **check**: `tests/test_serve.c` compares every command's stdout byte for byte with the command run alone, and runs natively in `make check` (`tools/native_tests.sh`): red on both before the fix, green after | closed | `test_serve`, first native run |
| 130 | 2026-09-23 | error | **a `--status` kept an unused server alive, and a test passed on the log of the run before**: the idle clock of `trochilus serve` restarted at every connection, so a script (or a test) polling `serve --status` kept a server with no command and its model's RAM up forever; and `test_serve` waited for "idle ... exiting" in a log it had not removed, so it read the previous run's line, then polled a server that could never go idle (1 failure in 3) | "idle" defined by the endpoint instead of by the use; a file a test reads, left from the run before | **check**: the idle time counts commands only (`--status` and `--stop` do not reset it); `test_serve` removes its logs before starting a server, and asserts the idle exit within 10 s while polling `--status` the whole time (6 runs of 6 green) | closed | `test_serve`, flaky on Linux |
| 131 | 2026-09-23 | discovery | **a prediction held in its mechanism and missed its size, and the measurement cannot say why**: question 49 predicted "misses warm = misses cold" at half budget (a store smaller than one sweep is rewritten by every prompt); measured 139 fewer of 1 157 (12%), 0.96 s of 13.00. The store reports one total per run, prompt and generation together, so where the 139 are (the previous run's decode units of the first layers, is the guess) stays a guess | a counter that sums two phases answers neither; the prediction reasoned about the prompt and the run measured prompt plus 8 generated tokens | open: the `experts:` line with prefill and decode apart, then the same session again; *rule*: a prediction names the counter that will judge it, and that counter measures only the phase the prediction is about | open | me, `tools/serve_first_prompt.sh` |
| 132 | 2026-09-23 | error | **`AB_WALL=1` turned a failed run into a fast one**: `ab_modes.sh` added the `wall:` line to the run's output before the check "a run that measured nothing stops the comparison" (#56), so that check could never fire: a run the engine refused, or Smart App Control blocked, entered the table as a ~45 ms `wall_ms` and the session ended with rc 0 (two failing modes gave `vs a 0.935x`, rc 0). The question 49 session was not touched: its runs all printed a prompt line | a new measurement appended to the data the old check reads, instead of after it; no test of `ab_modes.sh` at all | **check**: the wall line is added after the check; `tools/test_ab_modes.sh` in `make check` (failed and measured modes, with and without `AB_WALL`, 4 branch counters), red on a copy with the old order ("failed+wall: rc 0"); and a run that exits non-zero after its prompt line stops the comparison too (its exit status was ignored: the "crashed" branch, red without the check); the test unsets a caller's `AB_WALL`/`AB_GUARD` | closed | the reviewer agent, running it with fake modes |
| 133 | 2026-09-23 | error | **a warm run could silently be a cold one, and the log that would have said so was overwritten**: a client the server does not answer, or declines, runs the command itself with the same bytes, so `serve_first_prompt.sh` would have measured cold against cold and called it "warm = cold", the half-budget prediction; and the server's `--status` it appended to the server's log lost its first ~52 bytes (the server holds the log open without `O_APPEND` and wrote over it). The 2026-09-23 session is valid: the server's log lists 6 and 12 served `generate`, and warm misses (1 018) differ from cold (1 157) with no spread | a transparent fallback is also an invisible one to a measurement; two writers on one file, one of them not appending | **check**: `server_down` writes the status to a file of its own and fails unless the server says exactly one load and every warm run served; plus a `direct` preflight, bounded `--stop`, a server that dies while starting noticed at once, and `orphans.sh` knowing the script. *Rule*: a measurement of a mode that can fall back checks, from the other side's counters, that the mode ran | open until the next session runs the count check (its parser tested on a status line, the session not rerun) | the reviewer agent |
| 134 | 2026-09-23 | discovery | **the approved preview had an out-of-bounds read in its physics**: `build/progress-bar/preview.py` (variant A) can push the first cell's depth below 0 when the progress goes back, and then indexes its glyph table from the end (Python allows `EIGHTHS[-1]`); a faithful port to C reads before the array | a prototype in a language that forgives negative indices, ported line by line | **check**: `tests/test_bar.c` runs the physics on random schedules (progress going back included) and asserts every cell 0..8 eighths; a depth below 0 draws as an empty cell; red on the port without the clamp | closed | the implementing agent, the random-schedule test |
| 135 | 2026-09-23 | error | **a failure test that failed too late to test the failure path**: the failing-load test removed the last tensor the load reads, so the reported bytes had already reached the total and the bar was cleared by the "done" path; "a failed load clears the bar" stayed green with the failure path's close removed | a fault injected at the end of a sequence exercises the success path first | **check**: the test removes `blk.0`'s router (read early), and `tools/mutate_bar.sh`'s "close does not clear" mutant is red. *Rule*: inject a fault where the path under test is still ahead | closed | the implementing agent, under a mutation |
| 136 | 2026-09-23 | error | **naming three more measuring scripts in `orphans` made a measurement refuse itself**: `SET=prefetch sh tools/prefill_overlap.sh` launched from Claude Code's shell exited 6, "a measuring script pid 35532": the launcher `C:\Program Files\Git\bin\bash.exe -c "...tools/prefill_overlap.sh..."`, the caller's own grandparent. `orphans.ps1` excluded the MSYS callers and its own Windows parents, not the Windows parents above the outermost MSYS shell, where the chain continues through Git's launcher; the old list never met it because the scripts it named were launched another way | the exclusion followed one chain (MSYS) where there are two (MSYS, then Windows above it); a change to a pattern tested only against a detached fake, never against a real launch | **check**: `orphans.ps1` walks the Windows parents up from every caller; by hand, a real launch passes and a detached `sh -c '...' tools/prefill_overlap.sh` is still listed. Open: no automatic test launches through the Git launcher | open (the automatic test) | me, the measurement refusing to start |
| 137 | 2026-09-24 | error | **the server leaked a descriptor per malformed request**: a local client sending 4 descriptors instead of 3 left one open in the server per request, until it ran out | `recv_fds` (`src/app/serve.c`) closed what the kernel installed up to the 3 asked for, but `CMSG_SPACE(3 * sizeof(int))` rounds 12 bytes up to 16: the buffer holds a fourth, and the kernel installs it | **check**: `tests/test_serve.c` sends 2, then 4 descriptors and counts the server's `/proc/<pid>/fd` before and after (Linux), red before the fix ("the server holds 7, 6 before"). *Rule*: what a control buffer can receive is its size, not what was asked for | closed | the test agent's new case |
| 138 | 2026-09-24 | error | **a double free in the client on a reply of another protocol**: an endpoint answered by something that is not a Trochilus server aborted the client (exit 134) instead of running the command locally | `recv_reply` freed `*text` when the magic was wrong but left the pointer, and `serve_forward` freed it again | **check**: `tests/test_serve.c`'s fake server answering "XXXX" for "TRC1", red before the fix under gcc (`free(): double free detected`) and ASan (the two stacks). *Rule*: a function that frees an out-parameter on failure sets it to NULL | closed | the test agent's new case |
| 139 | 2026-09-24 | error | **a test premise that did not hold**: `test_serve`'s second synthetic model (1 layer instead of 2), meant to prove the server switches models, wrote the same six tokens as the first | with the same `n_embd` the two share embedding and output weights (the generator seeds by tensor order), and greedy decoding over a tiny vocabulary landed on the same tokens | **check**: the premise is itself checked (`b[7]` differs from `b[0]`), and the second model has a width of its own (`P2.n_embd = 64`). *Rule*: a synthetic model meant to differ changes a width, not only the depth | closed | me, the premise check |
| 140 | 2026-09-24 | error | **the server reloaded the model for the same file named with another case**: on Windows a client whose directory reads `C:\Users\marce\Desktop` and a server started in `...\desktop` gave two keys for one file, and every such request reloaded the model (5 s on the real one; the output stays right) | the model's key was `_fullpath` plus the file's stamp, compared with `strcmp`: Windows paths are not case-sensitive, and processes started by different parents (Git Bash, `start /d`, Python) spell the same directory differently | **check**: `tests/test_serve.c` (serve -m, a server started in another directory: 1 request, 1 load), red natively before the fix (`1 requests, 2 loads`, both keys printed); the key is now the name the disk gives the file (`GetFinalPathNameByHandleW`: case, 8.3 names and junctions resolved). *Rule*: a path used as an identity is canonicalized by the file system, never compared as typed | closed | the test agent's new case, natively (Linux could not show it) |
| 141 | 2026-09-24 | error | **a fixed endpoint name in a test run three times at once**: the gate failed on `serve_briefly() == 0` while the test alone was green 3 times of 3 | the 64-character name case used `azAZ09-_.000...0` for every process, and the gate runs `test_serve` under gcc, clang and ASan at the same time in one container: the second server found the first (`already running`) | **check**: the name carries the process's pid in its 55 digits, like every other endpoint of the test; the gate's parallel run is the check. *Rule*: every name a test gives the machine (endpoint, file, directory) carries its pid or lives in its own BUILD directory | closed | `make check`, parallel groups |
| 142 | 2026-09-24 | discovery | **the C tests spent 100 s of each build's 120 writing files**: `test_model_load` 66 s, `test_stream` 16, `test_gguf` 13 in every build, ASan barely slower than gcc; the same binaries copied into the container's `/tmp` ran in 0.4, 5 and 1 s | every test wrote its synthetic models next to its binary, and in the gate that is the Windows bind mount (9p): the 65535-expert case alone is tens of thousands of small reads across it. A timing table per gate step showed a block of two minutes, never a test | **check**: `TR_TEST_TMPDIR` (`tests/test.h` `tr_test_tmpdir`), set by the gate's builds to the container's own disk (`TEST_TMP`); the gate's table in MEASUREMENTS §The gate. `test_serve` keeps its models beside itself (`synth_write_in`): it names them relative to its directory, and with the variable set it had read the files a previous run left there | closed | me, timing each test in the gate's container |
| 143 | 2026-09-24 | error | **four `test_hot` at once wrote one file**: the gate failed on `synth_write(... "test_hot_prefetch.gguf" ...) == 0` right after the 20 runs of `test_hot` went four at a time | the lesson of #141 again, one level down: the runs shared one BUILD directory and one fixed file name | **check**: `synth_write` prefixes every file with the pid (any two runs of one test are apart, whoever starts them), and each of the 20 runs gets its own `TR_TEST_TMPDIR`; the gate's parallel run is the check (it was red) | closed | `make check` |
| 144 | 2026-09-24 | discovery | **the platform stamp could be read empty**: `platform-guard` rewrote `$(BUILD)/.platform` with `>` on every make, and the gate runs several makes on `build/linux-gcc` at once (two lanes until today, three now): one truncating the stamp while another reads it makes the second refuse the directory | a write that is not atomic, in a file read concurrently | **prevention by construction**: the stamp is written only when missing, through a temporary file and `mv` (atomic); taken from the unverified gate rewrite in the `trochilus-t3` clone, before deleting it | closed | the `trochilus-t3` clone's rewrite, read before deleting it |
| 145 | 2026-09-24 | error | **a `sed -i` wrote the two characters of barra+n into the Makefile**: adding a line after `dump_rope \` left `dump_rope \n<tab><tab>build/...dump_dequant` on one line | the replacement's escape went through the shell and sed differently than meant (the class of #14); the hook of #14 watched Python and shell writes, not `sed -i` | **check**: the hook `no-backslashes-in-shell-writes.cjs` also refuses a `sed -i` holding a backslash escape, seen red on the command that did it and green without the escape | closed | me, reading the line back |
| 146 | 2026-09-24 | discovery | **a check held only at its model's size**: `test_tier_used` required the F32 router to take the x4 road; with the Q4_K model (n_embd 256, whole 256-blocks) it did not | `tr_matmul_grouped` cuts its work in chunks of `4096 / cols + 1` products: at 256 columns the router's 11 tokens × 8 rows split in pieces with fewer than 4 whole tokens, one dot at a time (right: the router is small). The check was written at n_embd 64 | **check**: the router's x4 requirement stays at n_embd 64, where it holds, with the reason beside it; every model of the test still counts every product through the active table | closed | me, adding the Q4_K model to the test |
| 147 | 2026-09-24 | error | **a test's expected value had the wrong sign of zero**: the Q6_K dequant test computed `d * (sc * (q - 32))` with the product in integers, +0 where the definition `(d * sc) * (q - 32)` gives -0 (scale 0 times a negative quant); 12 elements red on the first run | the expected value written in another order than the definition's; bit equality sees the sign of zero | **check**: the expected value in the definition's order, with the reason beside it; `tools/check_dequant.py` holds the same -0 against gguf-py | closed | the test itself, on its first run |
| 148 | 2026-09-24 | error | **a mutation looked survived because its binary never ran**: native mutations counted `check failed` lines of a one-byte-longer copy; Smart App Control refused some copies (`Permission denied`), and mutations that did not compile (`-Werror`: an unused function, an address compared with NULL) left the old binary in place: 0 lines, "green" | a verdict read from the output, not from the exit code and the build | **check**: `tools/mutate_q6k.sh` runs in the container, judges by exit code, and prints `BUILD FAILED` for a mutation that does not compile (it did once: the mutation was fixed, 14 of 14 red) | closed | me, a shift mutation that could not be green |
| 149 | 2026-09-24 | error | **a Python replacement split a C string**: the vertical bar used as the line separator of a batch of replacements also stood in the text (the usage line `<q8_0 bar q4_k>`), and `dump_dequant.c` got an unterminated string | a separator that can occur in the text | **prevention**: multi-line replacements with the Edit tool, or separators that cannot occur in C (`@` in this repo's scripts); the build is the check (an unterminated string does not compile) | closed | me, reading the file back |
| 150 | 2026-09-24 | error | **a ratio concluded from noisy lines**: Q6_K on AVX2 written as 0.74–0.80× Q8_0 from a microbenchmark whose AVX2 lines had 19–56% spread (the text even said "±20%"); the second series, at ≤ 2%, gives 0.84–0.86× | the spread was read and the conclusion written anyway | **check**: `tools/bench_kernels.sh` names every line above 10% spread and says to remeasure before concluding (seen red: 28 lines of the first series, 3 of the second) | closed | Marcello asking to measure again |
| 151 | 2026-09-24 | discovery | **the weights' decoding does not bound the prefill's matmul**: Q8_0, Q4_K and Q6_K, 0.85–1.0× apart in the kernel microbenchmark, cost the same 0.062 ms per token through `tr_matmul`; the bound is loading the four input rows once per weight row. Two rows per load (`dot_row2_x4`): prefill 1.20–1.26× (Q8_0) | a microbenchmark with its inputs in L1 measured the decoding, the matmul streams its inputs from L2 | **check**: `bench_kernels` prints the whole matrix in every quantized type beside the kernel lines; `test_tier_used` requires every pair of rows through the two-row kernel (a mutation that left the last pair to x4 was green before) | closed | me, the new whole-matrix lines |
| 152 | 2026-09-24 | discovery | **OLMoE's attention is flat: no cached position can be skipped exactly.** In 65 M positions of real decode (prose, code, synthetic, ~2000 and ~4000 tokens, every layer and head) no score falls 30 below its max (exp reaches 0 at −103.97), two thirds sit between −10 and −5; even an oracle that knows every value skips at most 0.2% of the KV bytes. The whole family (exact zeros, absorption in the sum and in the output, keys or values split in high and low halves, norm, block and low-rank bounds) closed before a kernel | the QK-norm bounds every score | **check**: the premise is measured before a kernel on any model: `make attn-probe` + `tools/attn_skip_report.py`, whose replay must give the engine's bits first (8704 head outputs identical; two mutations of the replay red, 126 and 266 of 272) | closed | me, the premise measured before the kernel |
| 153 | 2026-09-24 | discovery | **a laptop GPU sleeps between two layers**: the decode's attention on the RTX 4070 Laptop takes 185 µs a layer at 2048 back to back and 390 µs (rising over a run, p90 754) with the CPU's 1.6 ms between calls: at 9-15% utilization the driver drops to P3-P4, the SM clock from 1605 to 255-345 MHz, the memory from 8101 to 6001 MHz | a benchmark that calls the kernels back to back measures a GPU the decode never sees | **check**: `tests/bench_gpu_attn.c time --only d` measures the decode's own pattern (bursts with a CPU gap), and the engine launches one warp spinning on `%globaltimer` after every decode call (`src/backend/gpu_attn.c`, TR_GPU_WARM_NS): P0 held, 185 µs flat; +12-17 W while decoding | closed | the premise agent, asked to measure the real duty cycle |
| 154 | 2026-09-24 | discovery | **ptxas fuses a PTX `mul.f32` and `add.f32` into an FFMA when they carry no rounding modifier**: the Q8_0 matrix-vector product on the GPU changes 75% of its floats | PTX without `.rn` lets the optimizer contract, like C without `-ffp-contract=off` | **check**: every float op in the engine's PTX carries `.rn`; the mutation that drops it is red in `tests/bench_gpu_q8.c --mutate norn`, `tests/bench_gpu_attn.c --mutate fma` and `tools/mutate_gpu.sh` | closed | the GEMV agent's mutation |
| 155 | 2026-09-24 | discovery | **the x4 kernels starve the prefetcher for a lone query**: `dot_f32_x4`/`axpy_f32_x4` read four rows 512 bytes apart a cache line of each in turn; for a decode token (nothing shared across the four) one position at a time reads each row as one ascending stream and the attention goes 1.10-1.13× faster, same bits (the same reads without arithmetic in x4 order run at 0.82-0.86×; prefetching every line 4 KiB ahead recovers the same) | a kernel built for a group of queries used for a group of one | **check**: `tr_attention_group` takes `n_q == 1` position by position; `test_attention_group` covers both branches (groups of 1 and of 2-19) against `tr_attention_head`; `tests/bench_attn_bw.c` measures the order | closed | the bandwidth agent's per-worker timing |
| 156 | 2026-09-24 | error | **the engine called an API whose implementation did not exist yet, while agents were building**: `model.c` and `olmoe.c` wired to `src/backend/gpu_attn.h` before `gpu_attn.c` was written, so every link of the tree failed (`tr_gpu_*` undefined) for the agents building their benches | the interface and its callers landed in a shared tree without a compiling skeleton | *rule*: an interface lands with a compiling skeleton (every function, failing cleanly) in the same step as its first caller, and `make WERROR=1 all` links before other work goes on | open | the GEMV agent, blocked at link |
| 157 | 2026-09-24 | discovery | **the benches' synthetic prompt falls into a loop**: `generate -p N` decodes into a period-2 loop by its 2nd-3rd token (found on the probe's dumps: layer 0's values identify the token), so speculation measured on it measures the loop (and a prompt lookup extended cyclically would look like a 2× lever there) | a synthetic prompt stands for text | *rule*: speculation is measured on text prompts (MEASUREMENTS §Speculation at long context uses prose, code and two tasks) | open | the brainstorm agent's token tool |
| 158 | 2026-09-24 | discovery | **after a prompt the GPU is cold, and a short generation pays its climb**: the engine's attention on the GPU took 652 µs a layer at 4000 in the A/B (the premise said 305-319) and 195-1079 µs at 2048-4000 from run to run; the module alone was linear and steady (302 µs at 4000, with real data, busy cores, RAM traffic in the gaps). The prompt's seconds leave the GPU at P5-P8 and its clocks take hundreds of ms to climb: over 48 decode tokens (the benches' generation) that is most of the run, over 400 it is not (315 µs) | the keep-warm started with the first decode call, after the GPU had already slept | **check**: the last pass of every multi-token eval launches the keep-warm warp after each layer's write (`tr_gpu_attn_warm`, olmoe.c `gpu_warm`): 4000 at 40 tokens 698-812 → 322 µs a call, 34.1 tok/s; `test_gpu_attn` runs its cases with that warp napping (bits); the decode_context A/B measures exactly the first 48 tokens after a prompt, where a regression shows | closed | me, the zone profile against the premise |
| 159 | 2026-09-24 | error | **the warm-up queued its warps**: the prompt's last pass called `tr_gpu_attn_warm` after every layer's write, each call a 200 ms warp on the same stream; stream-ordered, they ran one after another once no decode call ended them: up to 16 × 200 ms of spinning after a prompt with no decode (`logits -b`), the queue never draining during a generation, and `tr_gpu_attn_free` waiting for all of it | a launch per call on a stream whose previous work had not ended | **check**: `tr_gpu_attn_warm` launches only when the warm stream is idle (`cuStreamQuery`); `test_gpu_attn` calls it 16 times in a row and fails if the free after them takes 0.5 s (3.2 s queued) | closed | me, reading the code back while the A/B ran |
| 160 | 2026-09-24 | error | **a prediction taken from a bench run on a loaded machine**: the one-position attention was predicted at ~1.03× and ~1.05× a token from the bandwidth agent's 1.10-1.13× on the zone, measured while other agents' benches ran; on a still machine the engine before already read its KV at 49-52 GB/s and the token did not move (0.96-1.05×, A/A 2.9%; the zone −9% at 2048 only) | contention on the memory bus made the x4 order's penalty look big; the agent said its numbers needed a still machine, and the prediction used them anyway | *rule*: a prediction cites where its numbers were measured, and a number from a loaded machine is marked as an upper bound, never the expected gain | open | me, the short protocol's A/B |
| 161 | 2026-09-24 | error | **a peak loop written in C was at the compiler's mercy**: `tests/bench_peak.c`'s first loop started its twelve multiply and add chains from the same two values, and gcc merged them into two: the "peak" would have read 2× too high (seen in the disassembly before any run). The check then written for it (a peak below a real kernel fails the bench) sees only the opposite error: the mutant built from the bug survived it | common subexpressions: twelve chains of identical values are one chain to the compiler | **check**: the peak loop is inline assembly (the compiler can neither merge nor drop it); the check "a kernel faster than the peak" fails the bench, seen red on a mutant with dependent chains (`-DBENCH_PEAK_MUTATE`, exit 1) | closed | me, the disassembly, then the mutant |
| 162 | 2026-09-24 | discovery | **on Zen 4 the order of independent vector ops is worth 20%**: the peak's twelve zmm ops (6 multiply chains, 6 add chains, no dependence between them) strictly alternating mul, add, mul, add… run at 139 GFLOP/s on one core; grouped (6 mul then 6 add, or gcc's own order) at 167, on every repeat | the scheduler's pipe assignment (mul on two pipes, add on the other two) is not order-blind | *rule*: an assembly kernel is measured in more than one instruction order before it is kept (bench_peak's "mix" lines are the workshop) | open | me, measuring the peak |
| 163 | 2026-09-24 | discovery | **two premises predicted from a neighbour's number**: the Q4 draft's agreement (95–98%) was predicted from llama.cpp's int8 activations (KL 9e-3), and 4.5-bit weights sit at KL 3.4–5.8e-2, 88.7–94.7% agreement; the experts of a k-row pass (4.2–5× at 8) were predicted from a *time* ratio (2.8× at ~4 rows, which carries the rows' compute), and the count is 2.33× at 4, 3.34× at 8. The two errors cancel: the lever (~2× fewer bytes a token) survives | a number measured for another perturbation, or of another kind (time for count), taken as the premise's own | *rule*: a prediction names what its number measured; the premises now have their tools (`tools/draft_agreement.sh`, `tools/route_union_report.py`) to run on every new model and quantization | open | me, the premise measurements |
| 164 | 2026-09-24 | discovery | **quantized weights are nearly incompressible, and the only exact structure is the vocabulary nobody trained**: Q8_0 codes carry 7.59–7.69 bits of 8, Q4_K 3.83–3.87 of 4 (~7% and ~4% of the files with a lossless coder; predicted 12–20% and 5–12%); 220 rows of the output head are byte-identical to another row (Q4_K: 244, and 246 embedding rows of zero codes), layer 0's values are the only token-determined part of the KV | a scale that puts the block's largest weight at ±127 makes ~32 Gaussian weights fill the code range; under-trained tokens keep their initial rows | the scan is a tool (`tools/weights_genome.py`, `tools/kv_repeats_report.py`): it runs again on the next model | closed | me, the genome scan |
| 165 | 2026-09-24 | error | **a timing-only variant the compiler rewrote**: to price the Q8_0 scale's conversion in `dot_row2_x8`, a variant used a scale of 1.0f; gcc dropped the multiplies by one as well, and the kernel read 119 GFLOP/s against 100: a "15% cost of the scale" that was two multiplies per step, not the conversion. With a scale it cannot fold (block 0's, read at run time) the difference is nil | a constant chosen for convenience is a value the optimizer may use (the same family as #161) | *rule*: a timing-only variant uses values read at run time, and its disassembly is compared with the kernel's (the count of each instruction in the loop) before its number is read | open: *rule* | me, the numbers disagreeing with the pipe count |
| 166 | 2026-09-24 | discovery | **the lane tree was a tenth of a two-row call**: sixteen `tr_lane_combine` (a store, sixteen scalar loads, fifteen adds each) against 128 vector steps at 2048 columns; the same tree in SIMD, each level the even lane plus the odd one after it, is the same adds in the same order, bit for bit, and the x8 kernel goes 97 → 104 GFLOP/s. And the engine test's 11-token pass could never take the x8 road (three threads' chunks of ~3.7 tokens): its new counter went red the first time | the per-call tail was not in the microbenchmark's model of the kernel (only the stream was); the pass length was chosen for x4 | **check**: `tools/mutate_row2.sh` has the tree's mutations (a pairing, a swapped leaf in x4 and x8), all red; `test_matmul_grouped` counts the calls of each road exactly (x8, two-row x4, one-row x4, dots) and `test_tier_used` passes 29 tokens | closed | the counter, then the disassembly |
| 167 | 2026-09-24 | discovery | **integer work beside FP-bound loops is free**: the Q8_0 scale's scalar conversion (branches and all) costs nothing inside `dot_row2_x8`; every way of moving it (vcvtph2ps, converted ahead in a loop of its own, a gather, a ring in memory) was equal or up to 12% slower: they put ops on the FP pipes or took the overlap away | the loop is bound by the two multiply and two add pipes; the integer units are idle | *rule*: before moving work out of a hot loop, name the pipes that bound it (MEASUREMENTS §Two rows against eight tokens) | open: *rule* | me, the variants measured |
| 168 | 2026-09-24 | error | **#101 again, in the documents**: edits made with Python's `open(p, 'w')` on Windows turned `kernels_x86.c`, `STATUS.md`, `MEASUREMENTS.md` and `COMMANDS.md` to CRLF; the lint's line-ending check covered `src`, `tests`, `tools`, `bench` and `.claude`, not `docs`, and only STATUS's size cap (+468 bytes, one a line) made it visible | text mode translates every newline on Windows; the check had a hole where the documents are | **check**: `tools/lint.py` reads `docs/` and `CLAUDE.md` too (seen red on a probe file with a CR); *rule*: a Python edit opens files in binary, or with `newline=''` | closed | the STATUS size cap |
| 169 | 2026-09-24 | discovery | **the dequantized weight's product never depends on its order**: to see the new test (`dot_row == dot_f32(dequant_row)` on every tier) red, the Q4_K dequant was mutated to an FMA, and nothing failed: a half scale has 11 bits, a 6-bit scale and a 4-bit code make 21, so `d * sc * q` is exact in float whatever the order; Q8_0 (19 bits) the same, Q6_K (25 bits) rounds once in every order. Only Q4_K's `- min` rounds | an FMA or a reordering of the weight's product is an equivalent mutant, not a missed bug; the dot's fused multiply-add is not (it rounds the product with x) | **test**: `test_dequant_is_the_dots_weight` (tests/test_kernels.c), every tier, rows up to 4113, special values, a one-ulp panel must fail it per type; seen red under an FMA in the AVX-512 Q8_0 `dot_row` (2 of 10 rows); *rule*: a mutation of a dequant's product is not a test of it | closed | the orchestrator's mutation |
| 170 | 2026-09-24 | error | **a premise priced with a slower piece than the one it replaces**: question 63's first run (an agent, on the orchestrator's brief) decoded the panel with `dequant_row`, the scalar table's only entry, one weight at a time, against the x8 kernel's sixteen in AVX-512; the panel "lost" 0.58–0.70× at 64 tokens and the report said "do not build". With the decode in AVX-512 (the same floats) Q8_0 ties and Q4_K gains 1.1× | the brief named the function to call without asking what it costs; nobody timed the decode alone | *rule*: in a premise bench every part the engine would run in SIMD runs in SIMD, and the report prices each part on its own (decode, arithmetic) before the total; the orchestrator reads the agent's inner loop before accepting a "no" (CLAUDE.md step 6) | closed | the orchestrator's review of the agent's code |
| 171 | 2026-09-24 | error | **a gate step failed without a word**: `spec-check` stopped with only `Error 1`; the reason was in `spec-1.err`, which nobody printed (and `--spec 0`'s stderr went to /dev/null): the engine's memory guard refusing the whole model while the three model lanes ran beside another window's containers (7.6 of the Docker VM's 15 GB). Each lane rerun alone passed | a failure path that kept its message in a file | **check**: `spec-check` prints the engine's stderr of the run that failed (seen on a file that is not a GGUF: "not a GGUF file"); a gate beside other windows' containers can still run out of room: rerun the refused lane alone, and say so in the report | closed | the gate |
| 172 | 2026-09-25 | error | **an agent wrote seventeen test cases and ran none**: against `gguf.c`'s mutation survivors it wrote every case first, meaning to wire and run them at the end; the usage limit stopped it before, and wired by the orchestrator 6 fail on the real code (expectations never checked: a truncation inside the data section wants "unexpected end of file", which the reader rightly never says there) | a batch written blind, run once at the end | *rule* (in every test brief): each case is run as soon as it is written, green on the real code, then red on its mutant, before the next one; the work stays in `build/gguf_tests_wip.diff` | open | the orchestrator, wiring the agent's cases |
